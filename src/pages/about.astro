---
/**
 * About (faithful cutover) — delegates to the shared PageObjectRenderer, which
 * loads the published page_about object and renders its `about` section: the
 * full page markup, with the page/section headings, portrait image, and closing
 * CTA drawn from the CMS object. The prose furniture and structure are unchanged.
 */
import PageObjectRenderer from '~/components/cms/PageObjectRenderer.astro';
---

<PageObjectRenderer objectId="page_about" />
