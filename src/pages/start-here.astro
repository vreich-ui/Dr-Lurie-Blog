---
/**
 * Start Here (T4.3 cutover) — delegates to the shared PageObjectRenderer, which
 * loads the published page_start_here object and renders its `lede` section.
 * Copy now lives in the CMS object, editable by an agent; the markup is
 * unchanged.
 */
import PageObjectRenderer from '~/components/cms/PageObjectRenderer.astro';
---

<PageObjectRenderer objectId="page_start_here" />
