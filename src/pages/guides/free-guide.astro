---
/**
 * Free Guide (T4.3 cutover) — delegates to the shared PageObjectRenderer, which
 * loads the published page_free_guide object and renders its `lede` section.
 * Copy lives in the CMS object; the markup is unchanged.
 */
import PageObjectRenderer from '~/components/cms/PageObjectRenderer.astro';
---

<PageObjectRenderer objectId="page_free_guide" />
