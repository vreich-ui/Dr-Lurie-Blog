# T14.7 — Fix wave (scoped by T14.6's findings log)

**Phase 14 · depends_on: T14.6 · mode: auto · model: Opus 4.8 · effort: high**
**Blocks:** T14.9 (the repeatability proof should run on a fixed system).

## Read first

`w14-findings.md` (T14.6's output — the entire scope), ruling doc R8.

## Scope

- Work the findings log by severity: authorization wrongs first (any
  allow that should deny is CRITICAL), then broken functionality, then
  layout.
- One fix cluster = one commit, regression test included at the layer the
  bug lived.
- Re-run the matrix rows / audit steps each fix touches; tick findings
  off IN the log (finding → commit ref → verified).
- Per R8: judgment calls get made and recorded, not parked. A finding
  judged not-worth-fixing for V1 is marked `wontfix-v1` with one line of
  reasoning — visible, not silent.

## Acceptance criteria

- Findings log fully dispositioned (fixed+verified or wontfix-v1 with
  reason); suite green; both sites healthy. Committed `T14.7: …` (one
  commit per cluster); no PR.
