# T12.6 — HUMAN_GATE: capture acceptance run (Wolf reviews a real clone)

**Phase 12 · depends_on: T12.5 merged + deployed; T11.11 staging site live · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** W12 exit.

## Read first
`11-platformization-plan.md` §3 + §8 (W12 exit criteria), the OQ-W12-1/-2/-3 rulings, the T12.4/T12.5 run + report formats, `conversion-playbook.md` (run discipline; records rule).

## Context
The wave's proof: Wolf names an authorized target; the pipeline produces a reviewable draft clone inside the staging client; Wolf walks the result and the reports and signs off — or files the gaps.

## Scope — strictly sequential
1. Wolf names the target + confirms authorization + copy-rights posture in the run session (recorded verbatim in the run report).
2. Full pipeline: capture → map → theme → emit (staging site) → fidelity loop; zero manual store edits.
3. Wolf reviews: the draft pages in the staging preview, the fidelity report against the OQ-W12-2 bar, the gap report, and the copy-policy log.
4. Disposition per Wolf: accept (optionally publish/release selected pages on the staging site through the normal gates), or record the specific misses as W10-backlog entries / a composite-evidence update.
5. **Records (same change):** state-of-play entry with scores + refs; `11-platformization-plan.md` status flip; gap feed committed.

## Deliver
A signed-off, end-to-end demonstrated "point an agent at a site" capability with its limits documented — or a precise, evidence-backed list of what to grow next.

## Non-goals
- No unauthorized targets under any circumstances. No auto-publishing. No rubric-bending to pass.

## Acceptance criteria
- Run recorded with authorization quote, scores, and Wolf's disposition; docs updated in the same change.
- Committed `T12.6: …`; no PR.
