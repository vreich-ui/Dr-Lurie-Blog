# T13.10 — Seeds + roundtrip: `trk_drlurie` seed module and the ten-type `set_tracking` drill

**Phase 13 · depends_on: T13.2 · mode: auto · model: Opus · effort: high**
**Blocks:** T13.11.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §3 (seed values) + §14 (what the drill must prove), `docs/cms-architecture/conversion-playbook.md` (the factory recipe + trap table — binding), `scripts/lib/themes-seed-data.mjs` (the singleton-seed pattern), `scripts/lib/roundtrip-drill.mjs` + `roundtrip-reconcile.mjs` + `scripts/home-conversion-roundtrip.mjs`.

## Context

The conversion-factory leg: a seed module for the tracking singleton plus drill coverage so T13.11's credentialed run is pure driver work. Sandbox rehearsal publishes are EXPECTED to block at `export_commit_failed` (the sandbox success signal).

## Scope — files to create/modify

- NEW `scripts/lib/tracking-config-seed-data.mjs`: `trk_drlurie` body — posture `geo-adaptive`, `restricted_regions` = the RATIFIED EEA-30 + UK + CH list (OQ-W13-1, 2026-07-19), `honor_gpc: true`, banner copy, ALL pixel providers `enabled: false`, `own` enabled with `ingest_path '/api/t'`, `endpoint_env 'TRACKING_SINK_URL'`, `auth_env 'TRACKING_SINK_TOKEN'`, `blob_mirror 'fallback'`, defaults matrix per doc-12 §6; exports `CONVERSION_SEEDS`.
- `scripts/lib/roundtrip-drill.mjs` + `roundtrip-reconcile.mjs`: drill `set_tracking_config_fields` (with exact inverse) on the singleton; drill `set_tracking` (set → mutate → unset-to-null, inverses each step) on ONE object of EACH of the ten types; reconcile branch for the tracking family (meta-key lists updated).
- `scripts/home-conversion-roundtrip.mjs`: accept the new seeds file; local isolated-store rehearsal documented in the brief run-log.
- Records (same change — criterion 5 discipline for the PREP state): `object-inventory.md` tracking rows stay TODO/planned but gain the seed pointer; `state-of-play.md` session entry.
- Tests: `tests/scripts/roundtrip-*.test.mjs` extended for both new op drills; seed module shape-parses against `trackingConfigBodySchema`.

## Deliver

`node scripts/home-conversion-roundtrip.mjs --seeds scripts/lib/tracking-config-seed-data.mjs` runs clean against an isolated store: create → every permitted op drilled with exact inverses → validate clean → publish blocks at `export_commit_failed` (expected sandbox signal).

## Non-goals

No production run (T13.11, human_gate). No provider enablement in the seed (pixels ship disabled; enabling is a Wolf-era config edit through the normal verbs).

## Acceptance criteria

- `npm run check` + `npm test` green; drill tests cover all ten `set_tracking` targets; `node scripts/build-diff.mjs` EMPTY vs pre-task `main`.
- Committed `T13.10: …`; no PR.
