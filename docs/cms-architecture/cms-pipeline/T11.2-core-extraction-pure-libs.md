# T11.2 — Core extraction 1: schema + registries + grammar + validation (pure libs)

**Phase 11 · depends_on: T11.1 · mode: auto · model: Opus 4.8 · effort: high**
**Blocks:** T11.3, T11.4, T11.5.

## Read first
`docs/cms-architecture/cms-pipeline/w11-move-map.md` (T11.1's map — this task executes its pure-lib slice), `src/schema/**`, `src/lib/**` (registries, object-ids, patch grammar, approval/creation policy resolution, contract).

## Context
The pure-TS law layer has no runtime bindings — it moves first and cleanly: `src/schema`, `src/lib/registry`, `src/lib/object-ids*`, patch-op schemas/apply, approval/creation policy resolvers, the contract builder.

## Scope — files to create/modify
- Move the map's pure-lib slice into `packages/core/` (preserve git history: `git mv`); rewrite imports (site code imports from `@core/*` or the ratified alias); co-located tests move with their modules.
- `src/config/approval-policy.ts` + `src/config/creation-policy.ts` STAY site-side (they are per-client posture); core exposes the resolver + types and takes the config as input — verify no core module imports a site config directly.
- No behavior change of any kind: this is `git mv` + import paths.

## Deliver
The law layer lives in core; Dr-Lurie builds and tests green against it.

## Non-goals
- No server/function moves (T11.3). No renderer moves (T11.4). No de-hardcoding (T11.5). No API redesign "while we're here".

## Acceptance criteria
- Full suite + `npm run check` green; `astro check` 0; **build-diff EMPTY** (byte-identical public output); history preserved (`git log --follow` works on moved files).
- Committed `T11.2: …`; no PR.
