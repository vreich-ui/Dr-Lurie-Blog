# T9.25 — W9 records close-out

**Phase 9 · depends_on: T9.24 · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** — (last W9 task).

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` (stamp its status), `state-of-play.md` conventions, CLAUDE.md (the admin-surface descriptions that go stale with W9.g).

## Context
House rule: no work counts without its written record. W9 ends with the docs telling the truth.

## Scope — files to modify
- `docs/cms-architecture/10-admin-workspace-plan.md` — status header flipped to reflect reality (which waves shipped, which OQs resolved and how).
- `docs/cms-architecture/cms-pipeline/state-of-play.md` — close-out entry: what shipped, the three wave-exit records cross-referenced, standing caveats.
- `CLAUDE.md` — the admin-surface pointers updated (new route map, retired pages/functions, the two-tier rights model, where guardrails live now).
- `docs/cms-architecture/07-canvas-editing.md` — §4 hold annotated as lifted/closed by T9.19 (annotation, not rewrite).
- `docs/cms-architecture/cms-pipeline/queue.tsv` state alignment per runner convention.

## Deliver
Documentation that matches the built system; a newcomer session can orient from the docs alone.

## Non-goals
- No code. No retroactive editing of past session entries.

## Acceptance criteria
- Every W9 doc claim spot-checked against the repo (routes exist, deleted files gone, off-limits files untouched).
- `npm run check` green (prettier on touched md where applicable).
- Committed to the working branch. No PR unless asked.
