# T13.7 — `google_ads` + `ga4` adapters and the goal→conversion bridge

**Phase 13 · depends_on: T13.6 · mode: auto (OQ-W13-3 ANSWERED 2026-07-19) · model: Fable · effort: high**
**Blocks:** T13.8.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §7 (normative) + §2 (`goals` shape), OQ-W13-3 in §13 (ANSWERED 2026-07-19: full adapter set RATIFIED, GTM permanently OUT), `src/components/common/NetlifyOptInCapture.astro` (opt-in success — the `trk:goal` dispatch point), the checkout success surface (`src/pages/shop/thank-you.astro` + form_confirmation section).

## Context

Conversions fire ONLY from per-object `tracking.goals[].provider_conversions` declarations, through one bridge: activity → goal-map match on `(object_id, on)` → own `goal` event + each enabled provider's conversion stub. Google Ads is main-thread gtag with Consent Mode v2 (T13.6's defaults precede it by construction).

## Scope — files to create/modify

- NEW `src/lib/tracking/adapters/google-ads.ts`: `gtag('config','AW-…')` fixed snippet (pre-minified), `send_page_view:false`; conversion stub `(label, value_cents?)`; `cspHosts` (googletagmanager.com, googleadservices.com, google.com/pagead hosts).
- NEW `src/lib/tracking/adapters/ga4.ts`: optional `analytics`-class config; manual `page_view` on `astro:page-load`; goals mirrored as events.
- Bridge (in the loader, small): consume matrix activities + `trk:goal` CustomEvents; resolve against the goal map; emit own `goal` event; call enabled providers' stubs. `value_source:'product_price'` resolved at BUILD into the goal map from the product export — never client-guessed.
- Wire the three v1 goals end-to-end in fixtures: `purchase` (thank-you surface), `opt_in` (`NetlifyOptInCapture` success dispatches `trk:goal` — additive one-liner in that component, no behavior change to the capture itself), `contact_submit` (contact_form success).
- Enhanced conversions: OFF — assert no email-shaped data ever reaches gtag calls (test).
- Tests: bridge matching (object_id×on, multi-provider fan-out, disabled-provider skip), conversion fires only on declared goals, consent-gated ordering (denied region ⇒ conversion ping carries consent-mode state, never fires pre-bootstrap).

## Deliver

Fixture build: declared purchase/opt_in/contact goals fire own `goal` + gtag conversion with correct labels/values; undeclared activities never call providers.

## Non-goals

No server-side conversion uploads (recorded seam only). No GTM (ruled OUT — OQ-W13-3, 2026-07-19). No native pixels (T13.8).

## Acceptance criteria

- Provider set matches the OQ-W13-3 ruling (2026-07-19): full set, GTM out.
- `npm run check` + `npm test` green; `node scripts/build-diff.mjs` EMPTY vs pre-task `main`.
- Committed `T13.7: …`; no PR.
