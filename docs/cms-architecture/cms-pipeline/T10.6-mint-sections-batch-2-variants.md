# T10.6 — Mint section types, batch 2 + ratified bounded variants

**Phase 10 · depends_on: T10.5 · mode: auto · model: Opus 4.8 · effort: high**
**Blocks:** T10.8.

## Read first
Same set as T10.5, plus `docs/cms-architecture/09-template-system-plan.md` §5 (the named on-demand variant candidates: `steps.columns`, `cta_banner.compact`, `content_split.imageLayout`).

## Context
Second half of the ratified mint list (candidates: `timeline`, `comparison_table`, `video_embed`) plus the ratified **bounded variant fields** on existing types (e.g. `hero.variant: 'center'|'split'|'background'`) — enumerated values mapped to pre-built classes, rule 6 exactly.

## Scope — files to create/modify
- Batch-2 types: same per-type recipe as T10.5 (union member + registry module + component + registration + tests).
- Ratified variant fields: extend the existing union member schemas ADDITIVE-OPTIONAL (existing records/exports must parse unchanged — the M-9/`columns` precedent), component class-mapping per value, default = today's rendering byte-exactly, editor hints updated.
- `video_embed`-class types: sanctioned-host allowlist for embed URLs (reuse the repo-file-hotlink/trap-14 posture; no arbitrary iframes).

## Deliver
The full ratified vocabulary registered and render-proven; existing pages byte-identical.

## Non-goals
- No page/seed edits. No composite. No unratified variants.

## Acceptance criteria
- `npm run check` + `npm test` green; `astro check` 0; build-diff EMPTY; every new variant field proven additive (committed exports re-validate zero-blockers).
- Committed `T10.6: …`; no PR.
