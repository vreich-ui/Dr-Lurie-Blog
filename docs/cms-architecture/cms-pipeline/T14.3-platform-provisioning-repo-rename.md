# T14.3 — HUMAN GATE: platform Netlify provisioning + repo rename

**Phase 14 · depends_on: T14.2 · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** T14.4+. This step IS the "Netlify split ASAP" (R4).

## Read first

`decisions/2026-07-26-platform-site-ruling.md` (R4, R5),
`site-provisioning-runbook.md`, `secrets-runbook.md`.

## Context

Two account-authority actions in one sitting: (1) provision the platform
site's own Netlify project; (2) rename the GitHub repo → `platform` (R5:
assessed LOW pain — GitHub redirects, Netlify follows repo by ID, only
`GITHUB_REPOSITORY` env values need updating; tree pre-cleaned of literal
repo strings).

## Scope — ordered

1. Agent prepares: exact runbook checklist instantiated for `platform`
   (env table, store list), `--netlify-token` command ready.
2. **Wolf**: runs the token step (or hands the agent a token) — Netlify
   project created (name: Wolf's pick, e.g. `platform` or
   `<prefix>-platform`), 8 blob stores probed, auto-mintable secrets
   pushed. Completes the by-hand env items (Identity, ADMIN_EMAILS, build
   hook, GitHub binding). No DNS.
3. Seed drive through the front door on the new site: create → drill →
   publish → release; platform renders its starter pages at its URL.
4. **Wolf**: renames the repo → `platform` (one click). Agent updates
   `GITHUB_REPOSITORY` env value on BOTH Netlify projects, verifies
   Netlify↔GitHub linkage with one deploy each, sweeps
   docs/scripts/bundles for stragglers, records in state-of-play.
5. MCP endpoint of the platform site verified (`ping`, `tools/list`).

## Non-goals

No DNS/custom domain. No renaming `drluriescience` (R4 — explicitly not).

## Acceptance criteria

- Platform site live at its own URL, seeded, MCP answering; repo renamed
  with both deploys verified. All refs recorded. Committed `T14.3: …`.
