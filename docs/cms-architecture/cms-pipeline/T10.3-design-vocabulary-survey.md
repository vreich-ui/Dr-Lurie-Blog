# T10.3 — Design-vocabulary survey: reference targets → mint list + gap report

**Phase 10 · depends_on: — · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T10.4 (Wolf ratifies this task's output).

## Read first

`docs/cms-architecture/11-platformization-plan.md` §1.2, `docs/cms-architecture/design-principles.md` rules 1/6, `src/lib/registry/components/registered-types.ts` + `src/schema/bodies/section-v1.ts` (the current 21-member union), `docs/cms-architecture/09-template-system-plan.md` §5 (named on-demand variant candidates) and §8 (composite spec + OQ-W8-1…4).

## Context

Rule 1 grows the palette on demand; the multi-site/capture ambition is the demand. This task produces the evidence and the proposal — it mints nothing.

## Scope — files to create/modify

- ✚ `docs/cms-architecture/design-vocabulary-gaps.md`: analyze 2–3 reference sites (Wolf-named if provided in the session context; otherwise pick representative, publicly documented marketing/content-site patterns and SAY SO — do not crawl anything requiring auth). For each observed section shape: (a) expressible today (name the type + config), (b) expressible with a **bounded variant field** on an existing type (name field + enum values), (c) needs a **new reusable type** (propose name, data schema sketch, `useWhen`), or (d) inexpressible without composite arrangement → record as OQ-W8-1 evidence.
- Include: proposed mint list (recommend ≤ 6, batched ×2), proposed variant list, token-axis gaps vs T10.1's set, and the composite-evidence section (feeds T10.7).
- Every proposal passes the litmus test ("could an agent point this at different content / reuse it elsewhere without a code change?") — no per-page replicas.

## Deliver

One ratifiable document: mint list + variant list + axis gaps + composite evidence, each entry justified by a real observed pattern.

## Non-goals

- No code. No schema edits. No crawling machinery (W12 owns capture tooling).

## Acceptance criteria

- The doc exists, is internally complete (every observed shape dispositioned a–d), and ends with the explicit OQ-W10-1 question block for Wolf.
- Committed `T10.3: …`; no PR.
