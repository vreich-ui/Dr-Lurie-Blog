# T11.4 — Core extraction 3: renderer + admin workspace into core

**Phase 11 · depends_on: T11.2 (runs after T11.3 in queue order) · mode: auto · model: Opus 4.8 · effort: high**
**Blocks:** T11.6.

## Read first
`w11-move-map.md` renderer/admin slice; `src/components/**` (sections, PageObjectRenderer, CustomStyles, canvas overlay), `src/lib/renderer/**`, `src/pages/admin/**` + `src/components/admin-ui/**` + `src/lib/admin/**` (the W9 workspace), `src/utils/site-object.ts` (export-glob loading), `astro.config.ts` (react scoped to /admin).

## Context
The render law (components + registries + resolver) and the admin workspace are canonical product — every client gets them from core. Site-side keeps only the Astro shell: route files, config, exports, public assets.

## Scope — files to create/modify
- Move section components, PageObjectRenderer/resolve, CustomStyles, canvas overlay, admin pages' islands + logic into `packages/core/` per the map; `sites/drlurie/src/pages/**` become thin re-export shells over core page implementations (the `[...objectPage].astro` catch-all included).
- Data loading seam: core components receive the export root via the site shell (today's `src/data/site` globs move behind one loader module the site configures) — no core file may glob a site path directly.
- Admin stays per-site (OQ-W11-3 v1): each site deploys its own `/admin` from core components.

## Deliver
Core renders and administers; `sites/drlurie` is a shell; public output byte-identical.

## Non-goals
- No visual changes; no admin feature work; no multi-site console.

## Acceptance criteria
- Full suite + `astro check` green from the new layout; **build-diff EMPTY**; the standing leak-rule test (`private.*` never in DOM) still green.
- Committed `T11.4: …`; no PR.
