# T9.19 — Canvas document-body editing (the W7.7 remainder — LIFTS THE HOLD)

**Phase 9 · depends_on: T9.2 (token layer only — independent of the React workspace) · mode: notify · model: FABLE 5 · effort: high**
**Blocks:** T9.20, T9.21.

## Why Fable / high effort
This task edits `src/lib/edit-mode/ui.ts` (~2,500 lines of load-bearing vanilla overlay code) and binds TipTap to the restricted `rich_text.v1` grammar with exact round-tripping — the two highest-blast-radius moves in W9.f. Wolf's recorded hold on exactly this work ("no TipTap panel work … until that lands", 07-canvas-editing.md §4) is formally lifted by the W9 plan; honor its spirit by changing the overlay additively.

## Read first
`docs/cms-architecture/07-canvas-editing.md` (in full — the overlay spec), `docs/cms-architecture/10-admin-workspace-plan.md` §5 rows 2/6/7, `src/lib/richtext/rich-text-v1.ts` + `src/lib/richtext/prosemirror.ts`, `src/lib/edit-mode/ui.ts` (panel + preview mechanics), the `update_node`/`set_article_meta` ops in `src/schema/object-patch-ops.ts`.

## Context
The canvas can edit node copy but not article document bodies: title/lede and `rich_text.v1` node bodies. This is publish-page capabilities #2/#6/#7's new home.

## Scope — files to create/modify
- `src/lib/edit-mode/richtext-editor.ts` (NEW) — TipTap 3 instance bound to the grammar (p, br, strong, em, a[https only], ul, ol, li, h2, h3); toolbar offers ONLY grammar marks/nodes; serialize ↔ `rich_text.v1` via `prosemirror.ts`.
- `src/lib/edit-mode/ui.ts` — the "Edit text" accordion panel upgrades to the rich editor for `rich_text.v1` node bodies (plain-string bodies keep the plain form); title/lede chips on the article header → `set_article_meta` via the existing `EditSession`.
- Ask-AI: extend the copy-only suggestion → amber preview → Accept flow to rich_text nodes (suggestion applies as a full-body replacement op with word-diff display, reusing the existing diff approach).

## Deliver
An admin edits an article's title, lede, and rich-text prose directly on the live page — the last capability that required `/admin/publish`.

## Non-goals
- No metadata/taxonomy panel (T9.20). No readiness tray work (T9.21). No React in the overlay. No grammar extensions.

## Acceptance criteria
- Round-trip property tests: TipTap doc → `rich_text.v1` → TipTap doc stable across the grammar; out-of-grammar paste is stripped client-side AND a hand-built violation is rejected server-side by `validateObject`.
- Manual canvas drive on the object-model demo article: title, lede, one rich node, one Ask-AI accept — publish → release → live.
- `npm run check` + `npm test` green.
- Committed to the working branch. No PR unless asked.
