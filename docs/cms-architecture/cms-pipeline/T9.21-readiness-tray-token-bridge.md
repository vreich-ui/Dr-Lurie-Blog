# T9.21 — Readiness in the tray, publish options, canvas token bridge

**Phase 9 · depends_on: T9.19 · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** T9.23.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §5 rows 9/10 + §1 principle 3, `src/lib/edit-mode/ui.ts` (pending tray, `STYLES` const / `--dlem-*` tokens), `src/lib/admin/readiness-criteria.ts`, `src/assets/styles/admin-tokens.css` (T9.2).

## Context
Three small finishers: the publish-readiness gate moves into the canvas tray (capability #9), publish gains the explicit-timestamp option (#10), and the canvas token layer joins the shared semantic vocabulary.

## Scope — files to modify
- `src/lib/edit-mode/ui.ts` — tray rows gain a readiness summary (from `validate` before Publish); blockers disable the per-object Publish button with the criterion text inline; Publish offers "now" or an explicit timestamp → `publish_by_time` (no scheduling/unpublish — surface only what the verb allows, OQ-2).
- `STYLES` / token bridge — re-point `--dlem-*` custom-property VALUES at the `--adm-*`/`--aw-*` semantic layer. Values only; zero selector or behavior change.

## Deliver
The canvas tray gates publish on readiness exactly as the old page's readiness strip did, and canvas + workspace share one token vocabulary.

## Non-goals
- No tray redesign; no React in the overlay; no new verbs.

## Acceptance criteria
- A blocked object's Publish is disabled with the human criterion text; a clean object publishes with "now" and with an explicit time.
- Visual spot-check: canvas overlay renders identically (token re-point is invisible), light + dark.
- `npm run check` + `npm test` green.
- Committed to the working branch. No PR unless asked.
