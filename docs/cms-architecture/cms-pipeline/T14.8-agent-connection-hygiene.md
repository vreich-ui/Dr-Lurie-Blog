# T14.8 — Agent-connection hygiene (clean agent editing, proven)

**Phase 14 · depends_on: T14.7 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T14.9.

## Read first

`secrets-runbook.md` (per-agent tokens + the PUBLISH_SECRET rotation
procedure), `packages/core/server/lib/agent-keys.ts`, T11.10's
state-of-play entry.

## Scope

1. Mint per-agent keys (T11.10 mechanism, Owner action via
   admin-governance verbs) for the real fleet agents on BOTH sites;
   record which agent holds which (names only — never tokens).
2. Repoint/verify every MCP connector: each agent connection
   authenticates with ITS key, attribution lands verified in history
   (spot-check one write per agent).
3. Round-trip drill green per site under verified identity.
4. **PUBLISH_SECRET rotation** (standing debt since 2026-07-11): agent
   prepares per the runbook's five steps; Wolf executes the env change;
   agent verifies one real write end-to-end after. R8: this is the
   natural sitting — do not park it again.
5. Deprecation note: shared `MCP_HTTP_AUTH_TOKEN` flagged as fallback in
   the manual's credential page.

## Acceptance criteria

- Every active agent connection verified + attributed; rotation done;
  drills green both sites. Committed `T14.8: …`; no PR.
