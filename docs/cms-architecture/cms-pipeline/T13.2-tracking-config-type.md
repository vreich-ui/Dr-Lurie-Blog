# T13.2 — `tracking_config`: the eleventh governed type (`trk_drlurie`)

**Phase 13 · depends_on: T13.1 · mode: auto · model: Fable · effort: high**
**Blocks:** T13.3, T13.10.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §3 (normative body + governance), the W8.3 theme-type file surface (commit `9bbba9c` — the add-a-type checklist this task copies), `src/schema/bodies/theme-v1.ts`, `netlify/lib/materializers/` + `netlify/lib/materialize.ts`, `src/content/config.ts` (singleton collections `site.json`/`taxonomy.json`), `src/lib/object-ids.ts`, `src/config/creation-policy.ts` + `src/config/approval-policy.ts`.

## Context

The per-project tracker registry (12-plan §3): fixed-key provider blocks with regex-pinned IDs (never script/URLs — the `checkBrandTokenValue` law), a consent block, and the per-type defaults matrix. Singleton per site, `trk_<project>` naming. Publish is the site-wide script switch, so governance is pinned tight (pending OQ-W13-2 ratification, config defaults set as recommended).

## Scope — files to create/modify

- `src/schema/object-record-v1.ts`: add `'tracking_config'` to `objectTypes`.
- NEW `src/schema/bodies/tracking-config-v1.ts`: `providers` (own/ga4/gtm/google_ads/meta_pixel/taboola/outbrain/mgid/plausible — fixed-key strict, per-provider ID regexes + `consent_class`; `own` carries `ingest_path`, `endpoint_env`/`auth_env` env-var NAMES `/^[A-Z][A-Z0-9_]{2,63}$/` + no-`://` refinement, `sample_rate`, `batch`, `blob_mirror`), `consent` (posture enum, `restricted_regions`, `honor_gpc`, banner strings), `defaults` (the §6 matrix + `outbound_links`/`utm_capture`). `enabled:true` ⇒ id non-empty.
- `src/schema/object-patch-ops.ts`: `set_tracking_config_fields` (open deep-merge, `set_site_fields` idiom, no forbidKeys) + allowlist entry (this type does NOT get `set_tracking` and does NOT carry the attribute).
- `src/lib/object-ids.ts` / `object-ids-mint.ts`: `trk_` prefix, ceiling + `/^trk_[a-z0-9]+(?:_[a-z0-9]+)*$/`.
- `netlify/lib/object-validate.ts`: `BODY_SCHEMAS` entry + `tracking_config_ready` publish criterion (enabled⇒id; banner copy present when any advertising provider enabled and posture ≠ us-first; regions non-empty under geo-adaptive).
- `netlify/lib/object-verbs.ts` + `netlify/functions/mcp.ts`: singleton create-refusal (the `site` rule); tool descriptions.
- `src/lib/object-patch-apply.ts`: op apply + inverse.
- `netlify/lib/materialize.ts` + NEW `netlify/lib/materializers/tracking-config.ts` → **`src/data/site/tracking.json`** (singleton-filename convention; snake_case meta).
- `src/content/config.ts`: `trackingConfigObject` collection (glob `tracking.json`, base `src/data/site`), synchronous loader.
- `src/config/creation-policy.ts`: `tracking_config: { agents: [] }`. `src/config/approval-policy.ts`: `tracking_config: 'require-approval'`.
- `src/lib/registry/object-contract.ts`: full contract (creatable=humans, publish policy, constraints).
- Tests mirroring the theme set: schema, ids, patch/inverse, contract, materialize dispatch + determinism, validate criteria, singleton refusal.

## Deliver

`object_contract('tracking_config')` complete; create→patch→validate→publish path works in the sandbox store (publish blocks at `export_commit_failed` — the expected sandbox signal); export lands at `src/data/site/tracking.json`.

## Non-goals

No renderer/consumer of the export (T13.5). No seed record in production (T13.10/T13.11). No ingest (T13.3).

## Acceptance criteria

- `npm run check` + `npm test` green with the new test set; `node scripts/build-diff.mjs` EMPTY vs pre-task `main` (no export exists yet).
- Committed `T13.2: …`; no PR.
