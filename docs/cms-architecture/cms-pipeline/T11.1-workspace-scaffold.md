# T11.1 — Monorepo workspace scaffold (no moves yet)

**Phase 11 · depends_on: T11.0 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T11.2.

## Read first
`docs/cms-architecture/11-platformization-plan.md` §2.2 (target layout, as ratified at T11.0), `package.json`, `astro.config.ts`, `netlify.toml`, `.github/workflows/actions.yaml`, `tsconfig.json` + `tsconfig.test.json`.

## Context
Stand the workspace skeleton up while the code stays where it is — every later extraction task then moves files into a structure that already builds.

## Scope — files to create/modify
- Root `package.json`: npm workspaces (`packages/*`, `sites/*`); ✚ `packages/core/package.json` + ✚ `sites/drlurie/package.json` (placeholder manifests; the site depends on core).
- Keep the existing app building EXACTLY as today from its current paths (the root scripts keep working; workspace config is additive).
- `.github/workflows/actions.yaml`: teach CI the workspace install (`npm ci` at root) without changing what is built/tested yet; add a placeholder fleet-matrix job that currently builds only `drlurie` from the existing layout.
- ✚ `docs/cms-architecture/cms-pipeline/w11-move-map.md`: the authoritative file-by-file move map for T11.2–T11.6 (source path → target package), generated from the real tree — later tasks execute it instead of improvising.

## Deliver
Workspace-enabled repo, byte-identical behavior, and the move map every extraction task follows.

## Non-goals
- No file moves. No import rewrites. No netlify.toml behavior change.

## Acceptance criteria
- `npm ci` + `npm run build` + `npm test` + `npm run check` green from root; build-diff EMPTY; CI green with the workspace config.
- Committed `T11.1: …`; no PR.
