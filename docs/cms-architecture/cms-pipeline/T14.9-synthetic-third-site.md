# T14.9 — HUMAN GATE: synthetic third site, born on demand

**Phase 14 · depends_on: T14.8 · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** T14.10.

## Read first

Ruling doc (R7), `site-provisioning-runbook.md`, T14.3's recorded
execution (the pattern to repeat).

## Context

The repeatability proof: a made-up brand (agent invents name/palette/
copy), disposable after. Success is measured in Wolf's hands-on time —
the runbook checklist only — and the elapsed wall-clock, recorded as the
"cost of a new client" V1 baseline.

## Scope

1. Agent: full genesis prep (scaffold, brand values, seed pack, env
   checklist instantiated) — timed from start.
2. **Wolf**: the account-authority steps only (token step + by-hand env
   items). Timed separately.
3. Agent: seed drive, MCP round-trip, fleet CI now builds THREE sites,
   platform + drlurie unaffected.
4. Record both timings + refs in state-of-play. Optionally delete the
   synthetic site afterward (Netlify project + `sites/<name>/`) —
   deletion is itself a small proof the fleet tolerates tenant removal;
   record either way.

## Acceptance criteria

- Third site live (then optionally retired), timings recorded, fleet
  green at three. Committed `T14.9: …`; no PR.
