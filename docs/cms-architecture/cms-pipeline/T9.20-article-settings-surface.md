# T9.20 — Article settings surface (canvas + workspace)

**Phase 9 · depends_on: T9.19 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T9.23.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §5 rows 3/4, `src/lib/edit-mode/ui.ts` (accordion panel mechanics), `netlify/functions/admin-taxonomy.ts`, the `set_article_meta` op arg schema, `src/schema/bodies/content-item-v1.ts` (meta fields: author/date/category/tags/seo/slug).

## Context
Publish-page capabilities #3/#4: author, publish date, category, tags, SEO description, slug/path — editable where the article is edited.

## Scope — files to create/modify
- `src/lib/edit-mode/ui.ts` — an "Article settings" accordion section on article surfaces: author, date, category + tags via registry-backed pickers (suggestions from `admin-taxonomy`, resolved against `tax_drlurie` — never free-typed novel terms without flagging they'll need the registry), SEO description with counter, slug with contract validation feedback (uniqueness vs committed posts).
- Workspace parity: ensure the T9.9 generated inspector renders the same fields with the same pickers (shared validation messages).

## Deliver
Full article metadata editable from the canvas panel and the workspace inspector, both via `set_article_meta` under `EditSession`.

## Non-goals
- No taxonomy-registry editing (that's the taxonomy object's own workspace). No scheduling (OQ-2).

## Acceptance criteria
- Manual: set category/tags from registry suggestions → publish passes taxonomy enforcement; an unresolvable term is flagged before publish, not at it.
- Slug collision feedback appears at edit time (contract validate), not publish time.
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
