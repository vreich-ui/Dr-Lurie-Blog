# T9.23 — Parity sign-off (Wolf drives the capability checklist)

**Phase 9 · depends_on: T9.16, T9.20, T9.21, T9.22 · mode: human_gate · model: Sonnet 5 · effort: low**
**Blocks:** T9.24 — nothing is deleted before this passes.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §5 (the 11-capability table).

## Context
The retirement gate. The legacy pages are deleted only after Wolf confirms every `/admin/publish` capability works on its new surface.

## Scope
No product code. A sign-off checklist (the §5 table expanded into drive steps) committed under `cms-pipeline/`, driven by Wolf on production.

## Deliver
Wolf walks capabilities 1–10 on the new surfaces (draft discovery, title/lede, metadata, tags/SEO/slug, save/undo/discard, rich-node editing, Ask-AI diff accept, lock + takeover, readiness gate, publish now/timestamp) and rules on #11 (canonical-input promotion — OQ-W9-5, recommended: retire without port).

## Non-goals
- No fixes bundled — every gap found becomes a fix task that re-blocks T9.24.

## Acceptance criteria
- Each row checked off by Wolf (or explicitly waived), recorded in `state-of-play.md` with date and outcomes. OQ-W9-5 resolved in the same entry.
