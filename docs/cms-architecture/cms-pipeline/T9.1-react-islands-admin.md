# T9.1 — React islands, admin-scoped

**Phase 9 · depends_on: — · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T9.2, T9.12.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §3 (design system decision), `astro.config.ts`, `netlify.toml`.

## Context
The workspace (W9) is a stateful app — chat streams, approval cards, command palette — and is built as React islands **scoped to `/admin/*` pages only**. The public site and the canvas overlay stay vanilla. This task lands the integration and proves the scoping.

## Scope — files to create/modify
- `astro.config.ts` — add `@astrojs/react` integration.
- `package.json` — add `react`, `react-dom`, `@astrojs/react` (nothing else — no Radix/shadcn/cmdk).
- `tsconfig.json` — jsx settings as the integration requires.
- `src/pages/admin/kit.astro` (NEW) — admin-gated page (same client-gate pattern as `src/pages/admin/index.astro`) mounting one trivial React island.

## Deliver
- The integration compiles and the proof island hydrates on `/admin/kit`.
- No React runtime is loaded by any public page: no react chunks referenced outside `/admin/*` output.

## Non-goals
- No kit components (T9.2). No shell (T9.3). No changes to existing admin pages.

## Acceptance criteria
- `npm run check` + `npm test` green.
- `scripts/build-diff.mjs` **EMPTY** on public output (the reader site is byte-identical).
- Grep of `dist/` confirms react chunks are referenced only under admin pages.
- Committed to the working branch. No PR unless asked.
