# T13.5 — `TrackingScripts.astro`: the render seam (Layout swap, goal map, own+plausible adapters)

**Phase 13 · depends_on: T13.4 · mode: auto · model: Fable · effort: high**
**Blocks:** T13.6.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §4 (normative), `src/layouts/Layout.astro` (the head — where `Analytics` mounts today), `src/components/common/Analytics.astro` + `SplitbeeAnalytics.astro` (retiring), `src/config.yaml` (`analytics:` block, retiring), `src/lib/registry/theme-tokens.ts` (`checkBrandTokenValue` — the write+render double-enforcement to copy for ID interpolation), `src/content/config.ts` (collections read at build).

## Context

The single choke point where trackers attach. Reads the `tracking_config` export at build; renders NOTHING when none exists (the byte-identical gate for this task); otherwise emits config JSON + consent bootstrap skeleton + own loader + enabled vetted adapters. The renderer change that emits `data-cms-track="off"` for `tracking.enabled:false` objects also lands here.

## Scope — files to create/modify

- NEW `src/components/tracking/TrackingScripts.astro`: `/admin` bail; `getCollection('trackingConfigObject')` (absent ⇒ render nothing); assemble `<script type="application/json" id="trk-config">` — project id from the site export, enabled providers' IDs, consent block, defaults matrix, goal map `{object_id → goals[]}` aggregated from all collections' `tracking` fields (objects with goals or `enabled:false` only); emission order: consent bootstrap → loader `<script>` (imports `src/lib/tracking/loader`) → adapters.
- NEW `src/lib/tracking/adapters/own.ts` + `plausible.ts` (+ the adapter interface `(config, consent) → {head, cspHosts}` and the shared re-assert-regex-or-throw helper). Remaining adapters land in T13.7/T13.8.
- `src/layouts/Layout.astro`: replace the `Analytics` import/mount with `TrackingScripts`.
- DELETE `src/components/common/Analytics.astro` + `SplitbeeAnalytics.astro`; remove the `analytics:` block from `src/config.yaml` and the `@astrolib/analytics` dependency — **verify every importer first** (deletion gotcha; Splitbee is already import-free, Analytics has exactly the Layout mount).
- Section/node annotation sites (`src/lib/renderer/section-annotations.ts`, `src/lib/article-object/render-nodes.ts`): emit `data-cms-track="off"` when the owning object's `tracking.enabled === false`.
- Tests: component renders nothing w/o export; with a fixture export the dist page carries parseable `#trk-config` (post-astro-compress), the loader script, and ONLY enabled adapters; ID-regex render-throw; `data-cms-track` emission; goal-map contents.

## Deliver

With no `tracking_config` export (today's reality): build byte-identical to `main`. With a fixture export in a test build: config JSON + loader + own adapter present, plausible absent unless enabled.

## Non-goals

No banner/geo gating (T13.6). No pixel adapters (T13.7/T13.8). No CSP (T13.8).

## Acceptance criteria

- `npm run check` + `npm test` green; **`node scripts/build-diff.mjs` EMPTY vs pre-task `main`** (no export exists in-repo — the swap itself must be invisible).
- Committed `T13.5: …`; no PR.
