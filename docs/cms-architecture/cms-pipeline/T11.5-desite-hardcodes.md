# T11.5 — De-hardcode site identity (config, routing file, taxonomy/strategy ids)

**Phase 11 · depends_on: T11.2 · mode: notify · model: Fable · effort: high**
**Blocks:** T11.6.

## Read first

`docs/cms-architecture/11-platformization-plan.md` §2.3 (the coupling inventory — this task's checklist), `src/config.yaml`, `astro.config.ts` (site URL, image domains), `netlify.toml` (redirect table), `netlify/lib/taxonomy-enforcement.ts` (`TAXONOMY_RECORD_KEY`, line ~25), `src/schema/bodies/content-item-v1.ts` (`strategy_drlurie`), `netlify/lib/object-git-committer.ts` (fallback author). **Hard constraint reminder:** `publish-article.ts` + `admin-workflow-lock.ts` + the article MCP tools are OFF-LIMITS.

## Context

Core must contain zero Dr-Lurie literals; site identity becomes `sites/<client>/site.config.*`. Wolf B2 stands: routing authority stays a committed FILE — relocated per site, not objectified.

## Scope — files to create/modify

- ✚ `sites/drlurie/site.config.ts` (typed, zod-validated): site id, canonical host, metadata defaults/template, blog permalinks + bases, redirect table, image domains, git author fallback. `astro.config.ts` + the astrowind config loading read from it; `netlify.toml` redirects generated or verified against it (a drift test if generation is too invasive).
- Taxonomy id: `taxonomy-enforcement.ts` resolves the registry key from the site binding (convention `tax_<site>`, drlurie maps to `tax_drlurie`) — behavior for Dr-Lurie byte-identical, pinned by the existing hook tests. **`publish-article.ts`'s own references are NOT touched**: the legacy path stays drlurie-bound until its separate retirement; record this residual in the task summary.
- Strategy id in `content-item-v1.ts`: derive from site binding with the same drlurie mapping.
- ✚ Lint guard: an ESLint rule (or a `tests/scripts` grep test) failing on `drlurie` literals inside `packages/core/**` — allowlist nothing; site names live only under `sites/`. **Scope carve-out (ratified) below.**

## 2026-07-22 census — explicit target list (EXTENDS §2.3, does not replace it)

The 2026-07-19 §2.3 inventory is the spine; the code census on 2026-07-22
surfaced these additional Dr-Lurie couplings. Each must resolve through the
site binding (`src/lib/site-identity.ts` resolver — the single tenant seam).
**Most were front-loaded by the `pre-W11 dehardcode (1…9/11)` work** (branch
`claude/tenant-hardcodes-parameterize`, PRs #466/#467, commits titled
"pre-W11 dehardcode (N/11)"). Front-loaded items are marked
**FRONT-LOADED — verify absent** (confirm the literal is gone at task start,
per CLAUDE.md "check git, not the docs"); the rest are **PENDING**.

- **Admin-UI hardcodes:**
  - `Studio.tsx` theme-apply `SITE_ID` — **FRONT-LOADED — verify absent**
    (pre-W11 2/11: theme apply targets the resolver `siteId`; a client/env
    boundary note was also documented). Re-confirm no bare `SITE_ID`/`drlurie`
    literal remains in the apply path.
  - `ObjectWorkspace.tsx:180` + `src/lib/edit-mode/ui.ts:2346` `tax_drlurie`
    — **FRONT-LOADED — verify absent** (pre-W11 4/11: taxonomy lookups via
    resolver + source guard, ui.ts included).
  - `GovernancePage.tsx` `trk_drlurie` copy — **FRONT-LOADED — verify absent**
    (pre-W11 7/11: governance tracking-card copy via resolver).
- **`run-publisher-agent.ts`** site pin + branded agent instructions —
  **FRONT-LOADED — verify absent** (pre-W11 3/11: site id + brand from
  identity).
- **`track-ingest.ts`** `'drlurie'` fallback — **FRONT-LOADED — verify absent**
  (pre-W11 5/11: fallback via identity + loud warn).
- **MCP server identity strings** (`mcp.ts:121-122`) — **FRONT-LOADED —
  verify absent** (pre-W11 6/11: MCP server names from site identity,
  byte-identical).
- **`Favicons.astro` + `src/lib/registry/components/bio.ts`** kugelmedia URLs
  — **FRONT-LOADED — verify absent** (pre-W11 6/11: asset host via site
  identity — favicon + bio hint).
- **`pdf-tool-storage-grant.ts`** literal-typed `projectId` — **FRONT-LOADED
  — verify absent** (pre-W11 8/11: pdf-tool grant projectId per-site,
  wire-compatible). **Note: a SECOND tenancy axis** with its own env pair
  (`PDF_TOOL_PROJECT_ID`, distinct from the site slug / Netlify site id) —
  the resolver already models it (`pdfToolProjectId`); keep the two axes
  separate in the site config and the env table (T11.7).
- **GitHub `User-Agent` prefixes** — **FRONT-LOADED — verify absent** (pre-W11
  9/11: from site identity).
- **Agent-facing example ids in tool descriptions** —
  `src/lib/registry/object-contract.ts`, `netlify/lib/agent/tools.ts`, and
  `netlify/functions/mcp.ts` tool descriptions (`req_publish_drlurie_…`,
  `site_drlurie`, `thm_drlurie_default`, etc.) — **PENDING** (census
  confirmed ~9 `drlurie` occurrences still live in `mcp.ts`, plus
  `object-contract.ts`). These are agent-visible copy, not behavior: rewrite
  to a site-neutral placeholder or resolver-derived example so core carries
  no client-named ids. This is the one census item the pre-W11 front-load
  did NOT cover (items 10–11 of the planned 11 were never committed).

### Lint exit-bar carve-out (RATIFIED 2026-07-22)

The zero-`drlurie` core lint applies to **application code only**. `tests/`
fixtures are **EXEMPT** for v1 — parameterizing test fixtures away from
`drlurie` is **deferred**. Scope the lint rule (ESLint override or grep test)
to `packages/core/**` application sources and **exclude `tests/`**; the
§8/W11 exit criterion "zero `drlurie` literals in core (lint-enforced)" reads
against application code. (Decision record:
`docs/cms-architecture/decisions/2026-07-22-platformization-and-capture-rulings.md`.)

### OQ-W11-6 — `save-json-blob-mcp` is NOT in this task's remit (RATIFIED 2026-07-22)

`mcp/save-json-blob-mcp/` is **retired with the legacy article pipeline**
(T9.22/T9.24 line), **importer-check first** — it is **NOT extracted into
`packages/core`** and NOT de-hardcoded here. Treat it as legacy-bound like
`publish-article.ts`: leave its literals untouched and record the residual in
the task summary alongside the `publish-article.ts` note.

## Deliver

Core is site-agnostic (lint-enforced); Dr-Lurie behavior byte-identical through its config.

## Non-goals

- No off-limits file edits. No routing semantics changes. No second site yet.

## Acceptance criteria

- Full suite + hook tests green; build-diff EMPTY; the core-literals lint proves clean; residual legacy-path couplings enumerated in the summary.
- Committed `T11.5: …`; no PR.
