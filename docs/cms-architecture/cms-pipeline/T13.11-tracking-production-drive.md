# T13.11 — W13 credentialed production drive: `trk_drlurie` live, five criteria proven, pipeline observed end-to-end

**Phase 13 · depends_on: T13.6, T13.7, T13.8, T13.9, T13.10 · mode: human_gate · model: Fable · effort: high**
**Blocks:** — (wave exit).

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §14 (the proof list — normative), `conversion-playbook.md` §the credentialed-run recipe + trap 12 (schema-vintage: EVERYTHING merged + deployed to main BEFORE this run), T13.10's seed module + drill log, OQ-W13-2 (who executes the publish) and OQ-W13-6 (env provisioning).

## Context

The wave's human gate. An agent prepares everything; the human actions are: provisioning `TRACKING_SINK_URL`/`TRACKING_SINK_TOKEN`/`TRACKING_SALT` in Netlify env (per OQ-W13-6), executing the approval-gated publish of `trk_drlurie` (per OQ-W13-2), and signing off the live observations. Task is NOT done until the drive passes on production (house convention: nothing counts as shipped until the drive).

## Scope — the drive checklist (prepare as a committed checklist file, then run)

1. Pre-flight: schema-vintage check (deployed main carries all W13 schemas); env vars present; `object_contract('tracking_config')` + all ten contracts list the new ops from the DEPLOYED endpoint.
2. Mint + publish: seed-reconcile `trk_drlurie` into the production store (`--production`); human executes the require-approval publish; `release_to_production`; deploy ready.
3. Five criteria for `tracking_config`: renders (loader + own adapter in live HTML; pixels absent — all disabled), store-backed (`object_inventory`), round-trips (checkout → `set_tracking_config_fields` + inverse → publish → release via MCP), contract-complete, recorded (inventory 🟢 + conversion-map mark + state-of-play, same change).
4. `set_tracking` round-trip on ONE object of EACH of the ten types via MCP against production (set → verify export/render implications → inverse → re-publish as needed).
5. Live pipeline: real browser session on production — pageview/impression/dwell/click beacons observed at the sink (or mirror) with correct object identity, `project_id`, vhash daily rotation, consent flags; region-oracle answers; restricted-region simulation (VPN or header override) shows pixels held + Consent Mode denied defaults; GPC suppression verified; `/admin` emits nothing.
6. CSP: review the Report-Only soak (violation reports clean for the enabled set) → flip the header to enforcing in a follow-up commit if clean; otherwise record the exceptions and hold.
7. Wave exit: state-of-play wave-exit entry; OQ-W13 items updated with rulings; any drift found en route healed THROUGH THE STORE (never the export).

## Deliver

`trk_drlurie` CONVERTED per the five criteria; the tracking attribute proven round-trippable on all ten types; live beacons observed; CSP posture decided on evidence.

## Non-goals

No pixel enablement beyond what Wolf has ruled (OQ-W13-3); no reporting; no schema changes during the drive (abort → fix → redeploy → rerun instead).

## Acceptance criteria

- Every checklist step logged with real request/response evidence in the state-of-play entry; failures + heals recorded honestly.
- Records flipped in the SAME change as the successful drive; `npm run check` + `npm test` green on any follow-up commits.
- Committed `T13.11: …`; no PR unless the human gate asks for one.
