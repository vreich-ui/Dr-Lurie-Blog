# T10.7 — Composite-sections decision package (OQ-W8-1…4 evidence memo)

**Phase 10 · depends_on: T10.3 (uses T12.5 gap reports too when they exist) · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** — (any composite BUILD is a new wave, gated on Wolf's answers).

## Read first

`docs/cms-architecture/09-template-system-plan.md` §8 (the spec + the four OQs), `docs/cms-architecture/block-tree.md` (designed-never-built nested ops), `docs/cms-architecture/design-vocabulary-gaps.md` (T10.3's composite-evidence section + any W12 palette-gap reports referenced there).

## Context

Composite sections ("arrange blocks freely inside a section") are SPEC-ONLY behind OQ-W8-1…4. OQ-W8-1 demands evidence: three real layouts the bounded palette cannot express. The vocabulary survey and capture pilots produce exactly that evidence — this task assembles the decision memo. It decides nothing and builds nothing.

## Scope — files to create/modify

- ✚ `docs/cms-architecture/composite-sections-decision.md`: for OQ-W8-1, the named real layouts (screenshot refs / target citations) that survived T10.5/T10.6 (i.e. still inexpressible AFTER the mints); for OQ-W8-2/3/4, a concrete recommendation each (child vocabulary + depth cap; bounded-arrangement law; `content_grid.cards` coexistence) with the block-tree design cited; a build-cost sketch (the §8 law list: path-addressed ops with inverses, renderer dispatch, canvas addressing, `validateBlockTree` wiring).
- If the evidence does NOT clear OQ-W8-1 (fewer than three genuine cases), say so plainly and recommend composite stays gated — that is a valid outcome.

## Deliver

A memo Wolf can answer OQ-W8-1…4 from in one sitting.

## Non-goals

- No schema, op, or renderer changes. No new OQ numbering — the questions stay OQ-W8-\*.

## Acceptance criteria

- Memo committed; each of the four OQs has evidence + a recommendation + an explicit ANSWER LINE left blank for Wolf.
- Committed `T10.7: …`; no PR.
