# T11.11 — HUMAN_GATE: second-site acceptance run (the fleet proof)

**Phase 11 · depends_on: T11.7, T11.8, T11.9, T11.10 merged + deployed · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** T11.12, T12.4+ (the capture landing zone).

## Read first
`docs/cms-architecture/site-provisioning-runbook.md`, `packages/core/cli/create-site.mjs` `--dry-run` output, `docs/cms-architecture/conversion-playbook.md` (run discipline), `11-platformization-plan.md` §2.5.

## Context
W11's definition of done is a real second tenant. Prepared by agents; executed with account authority (Netlify site creation, env, DNS optional for staging).

## Scope — strictly sequential
1. `create-site --name staging` (or Wolf's chosen client name) — human completes the runbook steps (Netlify site, env table, secrets).
2. Store probe green; baseline seed pack driven in via the CLI (`--production` against the NEW site's endpoint): create → drill → publish → release; the staging site renders its seeded pages at its own URL.
3. Agent round-trip proof on the new site: one page created via MCP → patched → published → released; `object_inventory` (new site) returns it; Dr-Lurie's store/inventory shows ZERO change.
4. Fleet propagation proof: one trivial core commit (e.g. a comment) → fleet CI builds both sites; one REAL core change (pick a pending small fix) → both sites rebuild and Dr-Lurie's build-diff stays empty.
5. **Records (same change):** state-of-play entry; `11-platformization-plan.md` status header flip; CLAUDE.md platform note; inventory/conversion-map pointers where touched.

## Deliver
Two isolated tenants on one canonical core, with the update-propagation property demonstrated, recorded, and repeatable.

## Non-goals
- No public DNS/launch for staging. No content beyond the baseline pack + the proof page.

## Acceptance criteria
- All four proofs recorded with commit/deploy refs in state-of-play; both sites green in fleet CI.
- Committed `T11.11: …`; no PR.
