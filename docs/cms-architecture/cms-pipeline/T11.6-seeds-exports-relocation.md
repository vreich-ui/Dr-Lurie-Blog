# T11.6 — Seeds + exports relocation; CLI site-parameterization

**Phase 11 · depends_on: T11.3, T11.4, T11.5 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T11.7, T11.8.

## Read first

`w11-move-map.md` data slice; `scripts/lib/*-seed-data.mjs` (16 modules), `src/data/site/**`, `scripts/home-conversion-roundtrip.mjs` (endpoint default), `scripts/lib/roundtrip-reconcile.mjs`, `scripts/sync-site-seed.mjs` (+ its drift-guard test), `scripts/build-diff.mjs`.

## Context

Content-shaped assets move to the client: seeds and committed exports are Dr-Lurie data, not core. The driver/reconcile/sync/build-diff tooling becomes a core CLI taking a site root + endpoint.

## Scope — files to create/modify

- `git mv` seeds → `sites/drlurie/seeds/`, exports → `sites/drlurie/data/site/`; update the export-root loader seam (T11.4) and materializer output paths via the site binding (T11.3) — the publish path writes into the site's tree.
- Driver scripts move to `packages/core/cli/` with explicit `--site sites/drlurie` (and endpoint from the site config); the site-seed drift-guard test runs per site.
- Publish/materializer path tests updated; `__generated` markers unchanged.

## Deliver

Client data lives with the client; one CLI serves any site.

## Non-goals

- No seed content changes. No new sites yet.

## Acceptance criteria

- Full suite green; build-diff EMPTY; a local `--local --site sites/drlurie` driver rehearsal green end-to-end.
- Committed `T11.6: …`; no PR.
