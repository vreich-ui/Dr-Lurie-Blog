# T9.15 — Governance store + guardrails page

**Phase 9 · depends_on: T9.13, T9.4 · mode: checkpoint (OQ-W9-2 — do not start until Wolf answers) · model: FABLE 5 · effort: high**
**Blocks:** T9.16.

## Why checkpoint + Fable
OQ-W9-2 asks Wolf whether policy may move from commit-only (his one-file levers) to a runtime override store. If he says commit-only, this task shrinks to a read-only viewer. If he accepts the override layer, this is **security-boundary work** — it decides who approves what — and takes the Fable treatment.

## Read first
Wolf's OQ-W9-2 answer, `docs/cms-architecture/10-admin-workspace-plan.md` §7, `src/config/approval-policy.ts` + `src/lib/approval-policy.ts`, `src/config/creation-policy.ts` + `src/lib/creation-policy.ts`, `netlify/lib/publish-gate.ts` (consumer — read only), the T9.13 tool registry (chat_tools autonomy consumer).

## Context
The "adjustable guardrails" requirement: approval policy, creation policy, and chat tool autonomy on one Owner page, with committed config as the labeled default and disaster fallback.

## Scope — files to create/modify (override-layer variant)
- `netlify/lib/governance-store.ts` (NEW) — `governance` blob store, single doc `overrides.v1` `{ approval?, creation?, chat_tools?, updated_by, updated_at, history[] }`, validated with the SAME zod schemas as the committed configs.
- `resolveActivePolicies(store)` — store override if present AND valid, else committed config; consumed by the verb create/publish paths (where `activeCreationPolicy`/`activeApprovalPolicy` are read today) and the T9.13 tool loop.
- `netlify/functions/admin-governance.ts` (NEW) — read (Admin) / write (Owner); every write appends history.
- `src/pages/admin/settings/guardrails.astro` (NEW) — approval matrix, creation matrix, chat-tool autonomy table (auto/ask/off), provenance labels ("committed default" vs "runtime override"), one-click revert-to-committed.
- Tests: invalid/missing store doc falls back to committed config; Owner-only writes (Admin 403); publish/create paths honor overrides; revert works; chat loop honors an `off` tool.

## Deliver
Live, audited, Owner-adjustable guardrails with structural fallback to Wolf's committed defaults.

## Non-goals
- No change to the committed config files' semantics; no per-agent credentials (OQ-3); no UI for env allowlists.

## Acceptance criteria
- All tests above green; manual: flip a type to require-approval → agent publish blocks with the correct denial code; revert → autonomous again.
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
