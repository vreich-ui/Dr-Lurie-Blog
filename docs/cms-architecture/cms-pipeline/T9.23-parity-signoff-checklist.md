# T9.23 drive — parity sign-off (Wolf drives the §5 capability table)

**Status: PREPARED (2026-07-19). Waiting on Wolf.** The retirement gate:
T9.24 deletes nothing until every row below is checked (or explicitly
waived) by Wolf on production, recorded in `state-of-play.md` with date and
outcomes, and OQ-W9-5 is ruled in the same entry. **No fixes bundled — every
gap found becomes a task that re-blocks T9.24.**

Depends on: T9.16 drive complete; T9.20/T9.21/T9.22 deployed (this branch).

| #   | Legacy `/admin/publish` capability            | Drive it on the new surface                                                                                                                                                                                                                                 | ✓/waived |
| --- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | Draft picker                                  | `/admin/content` filtered to articles + the home attention inbox list drafts (unpublished changes / never published)                                                                                                                                        | ☐        |
| 2   | Title/lede editing                            | On the article page with `?edit=1`: title/lede edit in place (T9.19 canvas); same fields in the workspace Details drawer                                                                                                                                    | ☐        |
| 3   | Metadata: author / date / category            | **Category**: canvas panel → "Article settings" → registry-backed select; also in the workspace drawer. **Author**: NO object-model field exists (legacy frontmatter concept) — confirm retire-or-add here. **Date**: the publish timestamp option (row 10) | ☐        |
| 4   | Tags + SEO + path                             | Same "Article settings" section: tags with registry datalist + novel-term flag, SEO description with counter, slug with edit-time collision feedback (candidate validate BEFORE the lock)                                                                   | ☐        |
| 5   | Save with undo                                | Canvas: accept an edit → tray shows the pending change; Discard reverts (history inverses)                                                                                                                                                                  | ☐        |
| 6   | Per-node TipTap editing                       | Canvas "Edit text" on a rich_text node: toolbar restricted to the grammar; out-of-grammar content can't be entered                                                                                                                                          | ☐        |
| 7   | Per-node Ask-AI with word-diff accept/discard | Canvas Ask-AI on a node → amber in-place preview → accept/discard. ALSO §4a: the suggestion speaks through the object's ASSIGNED agent (swap the assignment, watch the provider flip — verify via function logs)                                            | ☐        |
| 8   | Lock lifecycle + force release                | Two sessions: second sees the lock banner; Owner "Take over" force-releases with history attribution                                                                                                                                                        | ☐        |
| 9   | Readiness strip gating publish                | Tray: a blocked object's Publish is DISABLED with the criterion text inline; the workspace strip above the composer shows the same                                                                                                                          | ☐        |
| 10  | `set_published_time` publish                  | Tray: Publish (now) works; the 🕓 affordance publishes with an explicit timestamp; scheduling/unpublish are absent (OQ-2 honored)                                                                                                                           | ☐        |
| 11  | Canonical-input promotion                     | **OQ-W9-5 ruling requested: retire without port** (variants + discard cover the intent). Wolf: **\_\_**                                                                                                                                                     | ☐        |

## Sign-off

- Every row above checked or waived by: ****\_\_**** on: ****\_\_****
- OQ-W9-5 ruling: ****\_\_****
- Findings → tasks (list): ****\_\_****

After sign-off, T9.24 (legacy deletion: publish/drafts/library/agent-admin/
review/objects pages + AdminNav + the §6 function list + blobs→maintenance
reskin) runs auto, followed by T9.25 records close-out.
