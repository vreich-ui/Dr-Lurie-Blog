# T9.23 drive — parity sign-off (Wolf drives the §5 capability table)

**Status: SIGNED OFF (2026-07-23).** The retirement gate has passed: every
row below is checked or waived, recorded in `state-of-play.md` (session
"T9.23 parity sign-off recorded"), and OQ-W9-5 is ruled in the same entry.
The sole gap found (row 3, author) became T9.23a, closed by PR #469 before
this sign-off. T9.24 (legacy deletion) is unblocked.

Depends on: T9.16 drive complete; T9.20/T9.21/T9.22 deployed (this branch).

| #   | Legacy `/admin/publish` capability            | Drive it on the new surface                                                                                                                                                                                                                                 | ✓/waived |
| --- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | Draft picker                                  | `/admin/content` filtered to articles + the home attention inbox list drafts (unpublished changes / never published)                                                                                                                                        | ✅       |
| 2   | Title/lede editing                            | On the article page with `?edit=1`: title/lede edit in place (T9.19 canvas); same fields in the workspace Details drawer                                                                                                                                    | ✅       |
| 3   | Metadata: author / date / category            | **Category**: canvas panel → "Article settings" → registry-backed select; also in the workspace drawer. **Author**: added (T9.23a, PR #469) — `content_item.author`, editable on both surfaces, optional byline. **Date**: the publish timestamp option (row 10) | ✅       |
| 4   | Tags + SEO + path                             | Same "Article settings" section: tags with registry datalist + novel-term flag, SEO description with counter, slug with edit-time collision feedback (candidate validate BEFORE the lock)                                                                   | ✅       |
| 5   | Save with undo                                | Canvas: accept an edit → tray shows the pending change; Discard reverts (history inverses)                                                                                                                                                                  | ✅       |
| 6   | Per-node TipTap editing                       | Canvas "Edit text" on a rich_text node: toolbar restricted to the grammar; out-of-grammar content can't be entered                                                                                                                                          | ✅       |
| 7   | Per-node Ask-AI with word-diff accept/discard | Canvas Ask-AI on a node → amber in-place preview → accept/discard. ALSO §4a: the suggestion speaks through the object's ASSIGNED agent (swap the assignment, watch the provider flip — verify via function logs)                                            | ✅       |
| 8   | Lock lifecycle + force release                | Two sessions: second sees the lock banner; Owner "Take over" force-releases with history attribution                                                                                                                                                        | ✅ (confirmed present — T9.4 force-checkin + LockBanner, prior credentialed runs) |
| 9   | Readiness strip gating publish                | Tray: a blocked object's Publish is DISABLED with the criterion text inline; the workspace strip above the composer shows the same                                                                                                                          | ✅ (confirmed present — T9.21, prior credentialed runs) |
| 10  | `set_published_time` publish                  | Tray: Publish (now) works; the 🕓 affordance publishes with an explicit timestamp; scheduling/unpublish are absent (OQ-2 honored)                                                                                                                           | ✅ (confirmed present — T9.21, prior credentialed runs) |
| 11  | Canonical-input promotion                     | **OQ-W9-5 ruling requested: retire without port** (variants + discard cover the intent). Wolf: **retire without port** — variants (`create_variant`) + Discard (history inverses) cover the intent; no object-model analogue needed. | ✅ waived |

## Sign-off

- Every row above checked or waived by: **Wolf** on: **2026-07-23**
- OQ-W9-5 ruling: **Retire without port** — canonical-input promotion has no
  object-model port; `create_variant` lineage + Discard cover the intent.
- Findings → tasks (list): **T9.23a — content_item author field (row 3's gap)
  — CLOSED, PR #469 merged 2026-07-23.** No other gaps found; no further
  tasks re-block T9.24.

After sign-off, T9.24 (legacy deletion: publish/drafts/library/agent-admin/
review/objects pages + AdminNav + the §6 function list + blobs→maintenance
reskin) runs auto, followed by T9.25 records close-out.
