# T9.11 — Attention inbox + audit feed

**Phase 9 · depends_on: T9.9 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** —.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §2 + §6 (audit feed row), `netlify/lib/object-inventory.ts`, the ObjectRecord `history[]`/`review`/`lock` envelope fields in `src/schema/object-record-v1.ts`.

## Context
The home page becomes the "what needs me" surface: pending reviews, unpublished changes, held locks, and a recent-activity stream — all read-only aggregation over existing record state; no new writes.

## Scope — files to create/modify
- `netlify/lib/audit-feed.ts` (NEW) — aggregate recent `history[]` entries across records (+ chat run outcomes once T9.13 lands — leave the join point ready) into a bounded, newest-first feed.
- `netlify/functions/admin-audit.ts` (NEW) — Admin-tier read-only endpoint.
- `src/pages/admin/index.astro` — fill the T9.3 placeholder slots: AttentionInbox (pending reviews w/ decide links, unpublished-changes list w/ per-object open/publish links, held locks w/ holder + age) and ActivityFeed (via `verbToPhrase` — human sentences, ids in tooltips).

## Deliver
A home page that answers "what changed and what needs me" at a glance.

## Non-goals
- No new history writes; no per-member audit view (T9.5 owns that); no pagination beyond a sane cap.

## Acceptance criteria
- Feed entries render as human phrases (spot-check against real seed history).
- Inbox reflects a live pending review + an unpublished change correctly (manual drive).
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
