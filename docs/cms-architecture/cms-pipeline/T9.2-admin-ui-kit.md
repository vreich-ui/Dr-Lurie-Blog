# T9.2 — Admin UI kit, tokens, display-name module

**Phase 9 · depends_on: T9.1 · mode: auto · model: FABLE 5 · effort: high**
**Blocks:** T9.3, T9.19 (token layer only).

## Why Fable / high effort
Every later W9 surface renders through this kit — its API shape, accessibility, and visual quality compound across ~15 downstream tasks. One heavy pass that lands right (consistent props, WAI-APG-correct interactions, dark/light correctness) beats iterated fix-up sessions. Set `/effort high` at session start.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §1 (principles) + §3 (component inventory), `src/lib/edit-mode/ui.ts` STYLES const (the canvas token approach to echo), `tailwind.config.js`, `src/components/CustomStyles.astro` (the `--aw-*` tokens), `src/lib/admin/readiness-criteria.ts`, `src/lib/admin/field-diff.ts`.

## Context
One design language for the whole workspace: a vendored, hand-rolled React kit on Tailwind + a semantic token layer derived from `--aw-*`. Also the identity rule made executable: a display-name module so no surface ever shows a naked ref.

## Scope — files to create
- `src/assets/styles/admin-tokens.css` — `--adm-*` semantic layer mapped from `--aw-*` (light/dark via the site's `.dark` class).
- `src/components/admin-ui/*` — the §3 inventory MINUS chat primitives (those are T9.14): Button, IconButton, Card, StatCard, Badge/StatusPill, DataTable, Tabs, Dialog/ConfirmDialog (typed-confirm variant), Drawer, DropdownMenu, CommandPalette, Toast, Skeleton, EmptyState, Breadcrumbs, Avatar, Input, Textarea, Select, Switch, TaxonomyPicker, DiffView (wraps `field-diff`), ReadinessList (renders `ReadinessCriterion[]` verbatim), LockBanner, HistoryTimeline.
- `src/lib/admin/display-name.ts` — `objectDisplayName(record)`, `verbToPhrase(historyEntry)`, `idTooltip(id)`; type-aware over all ten object types.
- `src/pages/admin/kit.astro` — expand into the full gallery: every component, every state.
- Tests: display-name over all ten object types; extractable component logic as plain-TS tests.

## Deliver
Kit + tokens + identity module + gallery. Native `<dialog>` for modals; WAI-APG keyboard patterns for menu/tabs/palette; no new deps.

## Non-goals
- No chat primitives (T9.14). No page beyond `/admin/kit`. No canvas `--dlem-*` re-point (T9.21).

## Acceptance criteria
- Gallery renders every component in every state, light + dark.
- `display-name` unit tests pass for all ten types (fixtures from real seed bodies).
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
