# T10.9 — HUMAN_GATE: W10 credentialed conversion run (new recipes + axis apply proof)

**Phase 10 · depends_on: T10.8 merged + deployed · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** W10 exit.

## Read first
`docs/cms-architecture/conversion-playbook.md` (the run recipe + trap table — mandatory), `docs/cms-architecture/09-template-system-plan.md` §W8.4 brief (the precedent this run copies, INCLUDING its live-palette-drift lesson: verify against the LIVE record at apply time, never the seed corpus).

## Context
No half measures: the new recipes count as converted only after the credentialed production run proves all five criteria. Prepared by an agent; executed with credentials (Wolf's machine or the session MCP connection when exposed).

## Scope — strictly sequential
1. Driver run `--production --seeds scripts/lib/section-templates-seed-data.mjs --release`: create the new `stpl_*`, drill every permitted op with inverses, publish, release.
2. Same for the new theme seed. Then `site_apply_theme` **dry_run** of the variant theme (do NOT real-apply a non-default theme to production); real-apply proof only if Wolf explicitly wants the look changed.
3. Verify store === seed === export per record; `object_inventory` recipe rows carry the metadata summaries.
4. **Records (same change):** `object-inventory.md` rows, `conversion-map.md` marks, `state-of-play.md` session entry, CLAUDE.md converted-count line.

## Deliver
New recipes CONVERTED (all five criteria) and recorded; the axis-aware apply path proven against production by dry_run.

## Non-goals
- No unratified seeds. No real apply of a non-default palette without an explicit Wolf instruction in the run session.

## Acceptance criteria
- All five playbook criteria per new object; docs flipped in the same change; run outcome recorded in state-of-play.
- Committed `T10.9: …`; no PR.
