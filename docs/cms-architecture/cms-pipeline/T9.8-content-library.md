# T9.8 — Content library + command palette wiring

**Phase 9 · depends_on: T9.3 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T9.9.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §2, `netlify/lib/object-inventory.ts` (what `inventory` returns — unpublished_changes, recipe summaries), `src/lib/edit-mode/verbs-client.ts` (`callObjectVerb` for `inventory`/`list`), `src/lib/admin/display-name.ts` (T9.2).

## Context
The human-named browse surface replacing raw-id lookups (the `/admin/objects` lookup form and the drafts table). Corpus is ~50 objects — client-side search over one inventory fetch is fine; no server search needed.

## Scope — files to create/modify
- `src/pages/admin/content/index.astro` (NEW, on AdminLayout) — type-filtered card/table view: display name, type badge, status (draft/published/review), readiness dot, unpublished-changes pill; sort by updated; empty states.
- CommandPalette wiring in AdminShell: open-object (fuzzy over display names), "Release to production", "New chat", nav jumps.

## Deliver
Browse → click → object workspace route (`/admin/content/<id>` — page lands in T9.9; until then link to `/admin/objects/<id>` is acceptable behind a TODO flag, or land T9.8+T9.9 adjacent).

## Non-goals
- No object workspace page (T9.9). No server-side search. No create flows (chat/studio own those).

## Acceptance criteria
- All ten types render with human names (no naked ids anywhere — ids only in tooltips).
- Palette opens with Cmd-K, fuzzy-finds by display name, and navigates.
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
