# T10.4 — CHECKPOINT: Wolf ratifies the mint list, variants, and token axes

**Phase 10 · depends_on: T10.3 · mode: checkpoint (OQ-W10-1 + OQ-W10-2) · model: Sonnet 5 · effort: low**
**Blocks:** T10.5, T10.6.

## Read first

`docs/cms-architecture/design-vocabulary-gaps.md` (T10.3's output), `docs/cms-architecture/11-platformization-plan.md` §1.1–§1.2 + §6.

## Context

Palette growth is deliberate, never speculative. Nothing mints until Wolf edits/approves the proposal — the same posture as the conversion-map priority table ("Wolf edits, agents follow").

## Scope — do not start until Wolf has answered

1. Collect Wolf's rulings on: the new-type mint list (OQ-W10-1), the bounded-variant list (OQ-W10-1), and the token-axis set/enum values (OQ-W10-2 — confirms or amends what T10.1 shipped as the default sketch).
2. Record the rulings verbatim-in-intent into `design-vocabulary-gaps.md` (a "RATIFIED (Wolf, date)" section: approved / deferred / rejected per entry) and update `11-platformization-plan.md` §1 if the axis set changed.
3. If Wolf amends the T10.1 axis set: file the delta as a follow-up note on T10.5's brief (small schema edit rides the next build task; do NOT silently rescope this checkpoint into a build task).

## Deliver

A ratified, dated mint/variant/axis list agents can build against without further interpretation.

## Non-goals

- No building. No unratified extras.

## Acceptance criteria

- Rulings recorded in the same change; the two build briefs' inputs are unambiguous.
- Committed `T10.4: …`; no PR.
