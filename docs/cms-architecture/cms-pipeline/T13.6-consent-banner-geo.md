# T13.6 — Consent: banner, geo-adaptive gating, GPC, Consent Mode v2 defaults

**Phase 13 · depends_on: T13.5 · mode: auto (OQ-W13-1 ANSWERED 2026-07-19) · model: Fable · effort: high**
**Blocks:** T13.7, T13.11.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §8 (normative — including the legal-accuracy notes carried verbatim) + §3 (`consent` block schema), OQ-W13-1 in §13 (ANSWERED 2026-07-19: geo-adaptive RATIFIED; restricted regions = EEA-30 + UK + CH; country+subdivision geo only; GPC global), T13.3's region-oracle contract.

## Context

The "legal but aggressive" mechanics: the cookieless own tracker needs no consent anywhere; `advertising`-class scripts auto-fire outside `restricted_regions` and are Consent-Mode-v2-gated inside; GPC always wins. Unknown region = restricted until the oracle answers. The banner is a code component; agents own only the validated copy strings in `trk_<site>`.

## Scope — files to create/modify

- Consent bootstrap (inline, emitted first by `TrackingScripts.astro`): `window.__trk.consent` state; region resolution (sessionStorage cache → `GET /api/t?mode=region` → the first pageview beacon response thereafter); `gtag('consent','default', …denied)` + `ads_data_redaction` + `url_passthrough` set BEFORE any vendor tag in restricted/unknown regions; activation of `<script type="text/plain" data-trk-gate="advertising">` on region-clear or grant; GPC (`navigator.globalPrivacyControl`) = ad-refusal + no id upgrade, always; `Intl` timezone heuristic may only KEEP scripts held, never release.
- NEW `src/components/tracking/ConsentBanner.astro`: copy from `tracking_config.consent.banner`; accept/reject/manage; `_dlconsent` localStorage `{analytics, ads, ts, region}`; fires consent updates + gated-script activation + the consented-id upgrade hook (loader mints `_dlid` only on analytics grant, 13-month cap); mounted by `TrackingScripts` when posture requires; re-open via a footer "privacy choices" nav link (navigation-object edit — document, don't hardcode).
- Posture variants honored from config: `geo-adaptive` (default), `consent-first`, `us-first` — one enum, all three testable.
- Tests: gate matrix (region × posture × GPC × grant) for script activation + consent-mode defaults; unknown-region hold; banner copy comes from config; no persistent id before grant; GPC beats grant.

## Deliver

Restricted-region simulation: pixels held, Consent Mode denied defaults present, banner shown, grant activates + upgrades id; unrestricted: pixels fire with no banner; GPC suppresses in both.

## Non-goals

No TCF CMP (recorded explicitly out of scope). No pixel adapters themselves (T13.7/T13.8) — gate against fixtures.

## Acceptance criteria

- Implements the OQ-W13-1 ruling (2026-07-19): geo-adaptive; restricted regions EEA-30 + UK + CH; unknown region = restricted; GPC beats grant everywhere.
- `npm run check` + `npm test` green; `node scripts/build-diff.mjs` EMPTY vs pre-task `main` (still no export in-repo).
- Committed `T13.6: …`; no PR.
