# T9.6 — Admin profile page

**Phase 9 · depends_on: T9.4 · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** T9.7.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §2, `netlify/functions/admin-users.ts` (`me`/`update_me`), `src/lib/edit-mode/verbs-client.ts` (`uploadImageArtifact` — the artifact upload pattern).

## Context
The "collect admin information" requirement: a self-service profile so members have names and avatars everywhere (chat attribution, history timelines, members table) instead of bare emails.

## Scope — files to create/modify
- `src/pages/admin/profile.astro` (NEW, on AdminLayout) — display name, avatar (artifact upload via the existing intent flow), preferences (timezone, notification placeholders); own role shown read-only with an explanation of tiers.
- `netlify/functions/admin-users.ts` — `update_me` field validation (display name length, avatar must be a trusted artifact ref); `last_seen_at` stamping on `me`.

## Deliver
A working profile page; display names/avatars flow into every kit surface that renders people.

## Non-goals
- No role self-change (Owner assigns roles in T9.5). No notification delivery (placeholder prefs only).

## Acceptance criteria
- Manual: edit name + upload avatar → members table and topbar chip reflect it.
- `update_me` rejects non-artifact avatar URLs (test).
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
