# T14.6 — Test plan + execution → findings log

**Phase 14 · depends_on: T14.4 · mode: notify · model: Fable · effort: high**
**Blocks:** T14.7.

## Read first

Ruling doc (known-issue log — seed #1 is the admin login, already fixed
or in-flight via T14.0); `secrets-runbook.md`; existing drills under
`scripts/drill-*` and `tests/`.

## Scope — the plan, then its execution, then a findings log

1. **Authorization matrix**: every server function + MCP verb × principal
   class (anonymous; agent w/ shared key; agent w/ verified per-agent
   token; editor; publisher; admin; owner) — expected allow/deny,
   automated where a seam exists, manual probe where not. Findings =
   any cell that answers wrong.
2. **Layout/visual audit**: every public page type + every `/admin`
   screen, BOTH sites, desktop + mobile viewport. Screenshot log.
3. **MCP connectivity**: per-site endpoint health, `tools/list` vs
   contract parity, per-agent key mint→use→revoke drill, session
   handling.
4. **Agent E2E drills**: existing round-trip drills re-run against
   production on both sites; content_item pipeline drill.
5. **Governance toggles**: approval/creation policy flips exercised and
   reverted.
6. Wolf's further observed issues fold in as they surface during his own
   testing (R8: capture and continue, never park).

Output: `docs/cms-architecture/cms-pipeline/w14-findings.md` — one row
per finding: surface, repro, severity, suspected cause.

## Acceptance criteria

- Plan executed across both sites; findings log committed; T14.7 scoped
  from it. Committed `T14.6: …`; no PR.
