# T10.8 — Starter recipe refresh: seeds exercising the new vocabulary

**Phase 10 · depends_on: T10.2, T10.6 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T10.9.

## Read first

`docs/cms-architecture/conversion-playbook.md` (traps + seed rules), `scripts/lib/section-templates-seed-data.mjs`, `scripts/lib/themes-seed-data.mjs`, `docs/cms-architecture/09-template-system-plan.md` §2.6 (self-contained seed rule) + W8.3b (metadata trio REQUIRED TO PUBLISH), `scripts/lib/roundtrip-reconcile.mjs` (recipe branches).

## Context

The recipe library should surface the grown vocabulary (REUSE-FIRST only works if recipes exist to reuse). New `stpl_*` seeds for the minted types, and at least one non-default theme exercising the token axes.

## Scope — files to create/modify

- `scripts/lib/section-templates-seed-data.mjs`: one starter recipe per minted type that warrants one (skip types with no sensible generic starter — say so in the seed comments), each self-contained (no object/asset/taxonomy refs), metadata-complete (description/whenToUse/scope), neutral starter copy.
- `scripts/lib/themes-seed-data.mjs`: ✚ one variant theme (e.g. `thm_editorial_airy`) with axis values set — scope `evergreen`, whenToUse explaining it is a demonstration preset. `thm_drlurie_default` stays byte-identical to production (the sync/drift-guard posture is untouched).
- Roundtrip driver support: verify the drill covers the new stpl records' three ops and the theme's `set_theme_fields` with the axis keys (extend `TEMPLATE_META_KEYS`-adjacent tables if needed).

## Deliver

Seed modules ready for the T10.9 credentialed run; local `--seeds` rehearsal green.

## Non-goals

- No production store writes (T10.9 is the credentialed run). No edits to live page seeds.

## Acceptance criteria

- Local driver rehearsal (`node scripts/home-conversion-roundtrip.mjs --local --seeds …`) all-green for the new seeds; `npm test` green; build-diff EMPTY.
- Committed `T10.8: …`; no PR.
