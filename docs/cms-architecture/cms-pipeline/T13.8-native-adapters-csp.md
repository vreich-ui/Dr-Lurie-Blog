# T13.8 — Native-platform adapters (meta/taboola/outbrain/mgid) + CSP Report-Only

**Phase 13 · depends_on: T13.7 · mode: auto · model: Opus · effort: medium**
**Blocks:** T13.11.

## Read first

`docs/cms-architecture/12-object-tracking-and-analytics.md` §4 (CSP + adapter interface) + §7 (native platforms), `netlify.toml` + `public/_headers` (no security headers exist today — this block is net-new), the T13.5 adapter interface + gating convention.

## Context

The remaining `advertising`-class adapters, all gated identically to google_ads, plus the site's first CSP — Report-Only until T13.11's soak, with a drift test so header edits ride the same change that enables a provider.

## Scope — files to create/modify

- NEW adapters `src/lib/tracking/adapters/{meta-pixel,taboola,outbrain,mgid}.ts`: fixed pre-minified snippets (fbq init+PageView; `_tfa` universal pixel; OB pixel; MGID sensor), regex-revalidated IDs, conversion stubs wired to the T13.7 bridge, `cspHosts` each (connect.facebook.net/facebook.com; cdn.taboola.com/trc.taboola.com; amplify.outbrain.com/tr.outbrain.com; MGID hosts).
- `netlify.toml`: `[[headers]] for = "/*"` with **`Content-Security-Policy-Report-Only`** — `default-src 'self'`; `script-src 'self' 'unsafe-inline'` (inline + astro-compress reality); `connect-src 'self'` + enabled adapters' hosts; `img-src`/`frame-src` per adapter; document the promotion path (T13.11 flips the header name after a clean soak).
- NEW test: union of ENABLED providers' `cspHosts` (from a fixture export and from `src/data/site/tracking.json` when present) ⊆ the netlify.toml policy — fails on drift in either direction (extra unexplained hosts also fail).
- Dist integrity tests (if not landed earlier): `#trk-config` parses post-compress; loader chunk within budget.
- Tests: each adapter's gate class, ID validation throw, conversion stub fan-in from the bridge.

## Deliver

All six pixel adapters exist behind config flags (all disabled in-repo); CSP-RO header present and consistent with an all-disabled provider set (i.e. effectively `'self'`-only plus nothing).

## Non-goals

No enforcing CSP (T13.11). No S2S conversion APIs. No Partytown flip (recorded escape hatch only).

## Acceptance criteria

- `npm run check` + `npm test` green; `node scripts/build-diff.mjs` EMPTY vs pre-task `main` (headers don't alter HTML).
- Committed `T13.8: …`; no PR.
