# T9.22 — Re-point the AI publisher at content_item (closes W7.5)

**Phase 9 · depends_on: T9.13 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T9.23.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §6 (re-point row), `netlify/functions/run-publisher-agent.ts` (the @openai/agents workflow), `netlify/functions/publish-article.ts` (**OFF-LIMITS — read for contract understanding only**), the content_item creation path in `netlify/lib/object-verbs.ts`, Wolf's OQ-W9-1 answer (ChatKit fate) if given.

## Context
The 5-agent drafting workflow still terminates in the legacy markdown `publish-article` path — a dead end since the post collection was wiped (W7). Its output belongs in content_item objects under the standard gate. Only the CALLER changes; the off-limits files stay untouched (Wolf's 2026-07-13 ruling: tools/functions may be re-pointed as W7 phases land, provided the functionality survives on the object substrate).

## Scope — files to modify
- `netlify/functions/run-publisher-agent.ts` — final stage targets the object substrate via `handleObjectVerb`: create content_item → node upserts (strategy annotations preserved into `private.*`) → taxonomy resolution against the registry → validate → publish under the existing gate; release NOT auto-fired (batch-publish, release-once discipline).
- If OQ-W9-1 = retire ChatKit: `/admin/agent-admin` + `create-chatkit-session.ts` retirement moves into T9.24's deletion list; otherwise leave both untouched.

## Deliver
An AI-published article lands as a governed content_item — visible in the workspace, editable on the canvas, released by the standard flow.

## Non-goals
- **No modification of `publish-article.ts`, `admin-workflow-lock.ts`, or the `save_json_blob_*` MCP tools.** No workflow-prompt redesign beyond the output re-target.

## Acceptance criteria
- A drive run produces a content_item whose nodes carry strategy annotations; taxonomy resolves; publish passes the gate; the object round-trips in the workspace.
- The legacy markdown path receives zero writes from this flow (grep + test).
- `npm run check` + `npm test` green.
- Committed to the working branch. No PR unless asked.
