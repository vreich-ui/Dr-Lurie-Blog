# T14.0 — Admin login fix (Wolf-reported, production)

**Phase 14 · depends_on: — · mode: notify · model: Fable · effort: high**
**Blocks:** nothing formally, but it gates every human step of W14 (Wolf
cannot approve/administer while locked out) — run FIRST.

## Read first

`decisions/2026-07-26-platform-site-ruling.md` (known-issue log — triage
already done, don't redo it), `packages/core/server/lib/admin-auth.ts`
(`getAdminStateFromEvent`), `packages/core/lib/admin/users-client.ts`
(`fetchMe` flow), `packages/core/admin/AdminShell.tsx` (client login path),
`netlify/functions/admin-users.ts` (shim).

## Context

Wolf reports login on drluriescience `/admin` does not work. 2026-07-26
triage: page renders, GoTrue backend healthy (email+google on,
autoconfirm off), `admin-users` boots (GET → 405). Fault is inside the
login round trip. Ordered hypotheses: (a) server-side JWT verify/role
resolution (`IDENTITY_URL`, users store, `ADMIN_EMAILS`); (b) browser-side
GoTrue exchange / island hydration; (c) unconfirmed Identity account.

## Scope

1. Audit the full login code path client→server; build a local repro
   (simulated Identity JWT against the real handler if a seam allows).
2. Use Netlify function logs + one observed browser symptom from Wolf's
   next attempt (he is testing anyway — not a parked question, R8) to pick
   the branch.
3. Fix; add a regression test at whatever layer the bug lived; verify a
   real login end-to-end on production.
4. Record root cause in state-of-play (was it a W11 regression or
   pre-existing?).

## Non-goals

No auth redesign; no role-model changes; smallest fix that restores login.

## Acceptance criteria

- Wolf logs into `/admin` on production. Regression test in the suite.
- Root cause recorded. Committed `T14.0: …`; no PR unless asked.
