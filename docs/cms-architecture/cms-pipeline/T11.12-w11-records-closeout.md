# T11.12 — W11 records close-out

**Phase 11 · depends_on: T11.11 · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** —.

## Read first
`docs/cms-architecture/cms-pipeline/state-of-play.md` (top entries), `CLAUDE.md` (reality lines), `docs/cms-architecture/object-inventory.md`, `conversion-map.md`, `11-platformization-plan.md`.

## Context
The house rule: no record = not done. W11 changed the repo's shape — every governing doc that describes paths, stores, or "the site" must describe the platform truthfully.

## Scope — files to create/modify
- `CLAUDE.md`: platform framing (core vs sites, where the hard constraints' files now live, updated mandatory-reading paths); keep Wolf's governing rulings verbatim.
- `state-of-play.md`: wave summary entry (if T11.11's entry didn't already carry it) + standing-state table refresh.
- `object-inventory.md` / `conversion-map.md`: path references updated; a "per-site inventory" note (each site's machine truth is ITS `object_inventory` tool).
- `cms-pipeline/README.md` + `queue.tsv` comments: runner instructions updated for the workspace layout (brief paths unchanged).
- Sweep `docs/cms-architecture/**` for dead paths introduced by the move (link check; fix or annotate).

## Deliver
Documentation that matches the platform reality, same-change discipline preserved.

## Non-goals
- No code. No history rewriting of past entries (they describe their own time truthfully).

## Acceptance criteria
- A link/path check over the docs tree passes; CLAUDE.md's hard-constraint file paths verified against the new tree.
- Committed `T11.12: …`; no PR.
