# T14.10 — V1 close-out + separation design doc

**Phase 14 · depends_on: T14.9 · mode: auto · model: Sonnet 5 · effort: low**
**Blocks:** — (end of W14; V1 line).

## Read first

Ruling doc; `state-of-play.md` W14 entries; `CLAUDE.md`;
`object-inventory.md`; `conversion-map.md`; old
`T11.12-w11-records-closeout.md` (its scope folds in here).

## Scope

- `CLAUDE.md`: platform framing (core vs sites, platform site's role,
  where the hard-constraint files live now, updated mandatory-reading
  paths). Wolf's governing rulings verbatim, R8 included.
- `state-of-play.md`: W14 wave summary + standing-state table refresh
  (V1 declared, with the honest list of wontfix-v1 items).
- `object-inventory.md` / `conversion-map.md`: path updates + per-site
  inventory note (each site's machine truth = ITS `object_inventory`).
- `cms-pipeline/README.md` + `queue.tsv` comments: runner instructions
  match the workspace layout; W12/W13 rows stand as the post-V1 queue.
- Dead-path sweep over `docs/cms-architecture/**` (repo rename +
  relocations); fix or annotate.
- **The separation design doc** (`docs/cms-architecture/13-separation-
  plan.md`): the later mechanism for splitting assets into per-client
  repos, Netlify projects, and domain names — versioned-core
  distribution, client-repo generation, migration order, the drift
  mitigation story. DOC ONLY — the design exists before it is ever
  needed.

## Acceptance criteria

- Docs match platform reality; link/path check passes; separation plan
  committed. Committed `T14.10: …`; no PR. **V1.**
