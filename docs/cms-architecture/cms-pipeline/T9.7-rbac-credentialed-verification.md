# T9.7 — RBAC credentialed verification (wave exit W9.b)

**Phase 9 · depends_on: T9.5, T9.6 · mode: human_gate · model: Sonnet 5 · effort: medium**
**Blocks:** nothing mechanically; W9.b is not "done" without it.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §8 + §10, the T9.4/T9.5 test suites (what is already proven headlessly).

## Context
House convention: security work isn't shipped until proven on the deployed site with real credentials. The agent prepares the script; the human actions (second account, real invite email) are the gate.

## Scope
No product code. A verification script/checklist committed under `docs/cms-architecture/cms-pipeline/` and executed against production or a deploy preview.

## Deliver — the checklist Wolf (or a second human) drives
1. Owner invites a fresh email (real inbox) → invite arrives → accept → first login flips `active`.
2. As the new Admin: content editing works; `/admin/settings/admins`, `/admin/settings/guardrails` (if landed), `/admin/maintenance` are hidden AND direct function calls return 403 (curl with the Admin's bearer token — server-side proof, not UI-hiding).
3. Bootstrap fallback: temporarily rename/suffix the users-store doc in a non-production store copy (or use the documented test hook) → `ADMIN_EMAILS` member still resolves Owner.
4. Lock takeover: Admin holds a checkout; Owner force-checkins; history shows the attributed takeover; Admin's next patch 409/423s correctly.
5. Disable the test account → all access revoked server-side.

## Non-goals
- No fixes bundled in — anything found becomes its own task.

## Acceptance criteria
- Every checklist line observed and recorded in `state-of-play.md` (date, who drove, outcomes) — the wave-exit entry.
