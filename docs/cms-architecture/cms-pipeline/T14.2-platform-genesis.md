# T14.2 — Platform-site genesis (in-repo half)

**Phase 14 · depends_on: T14.1 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T14.3.

## Read first

`decisions/2026-07-26-platform-site-ruling.md` (R2),
`docs/cms-architecture/site-provisioning-runbook.md`,
`packages/core/cli/create-site.mjs`.

## Context

The platform site (`sites/platform`, `site_platform`) is core's own
tenant: staging + administration + the instruction manual. This task is
the agent-executable half of its birth.

## Scope

- `create-site --name platform` (dry-run reviewed, then real). Adjust the
  scaffold's identity/brand values to the platform's role (name
  "Platform", neutral default theme is fine — the manual's content is the
  product, not the styling).
- Wire its build entry (per T14.1's pattern); local build renders the
  starter seed pack.
- Suite green including the scaffold tests; zero-drlurie lint untouched.

## Non-goals

No Netlify calls (T14.3 owns account authority). No manual content yet
(T14.5). No DNS.

## Acceptance criteria

- `sites/platform/` exists, builds, renders starter pages locally.
- Committed `T14.2: …`; no PR.
