# T9.24 — Legacy deletion + maintenance reskin

**Phase 9 · depends_on: T9.23 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T9.25.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §6 (retirements row), the T9.23 sign-off record, `netlify.toml` (redirects for `/admin/review/*` and `/admin/objects/*` — must be cleaned with their pages), CLAUDE.md gotcha: **file-deletion tasks require verifying every importer first.**

## Context
The payoff commit(s): the obsolete surfaces go away. One coherent deletion group per commit, each independently verified.

## Scope — deletion groups (importer-grep + `npm test` + `npm run check` + full build per group)
1. **Pages:** `src/pages/admin/publish.astro`, `drafts.astro`, `library.astro`, `review/[draftId].astro`, `objects/[objectId].astro`, `agent-admin.astro` (if OQ-W9-1 = retire), `src/components/admin/AdminNav.astro`, the "Legacy" nav group in AdminShell, and the now-dead `netlify.toml` redirects.
2. **Functions:** `admin-ask-ai-node.ts`, `get-article-for-edit.ts`, `admin-update-node.ts`, `admin-patch-workflow.ts`, `list-draft-articles.ts`, `admin-save-json-draft.ts`, `admin-get-json-draft.ts`, `admin-list-json-drafts.ts`, `toggle-article-publish.ts` (+ `create-chatkit-session.ts` if ChatKit retires). Orphaned libs/tests that only they imported go with them.
3. **Reskin:** `src/pages/admin/blobs.astro` → `src/pages/admin/maintenance.astro` on AdminLayout — human framing, raw JSON behind a "Raw" tab, wipe tools behind typed-confirm; Owner gate (server side landed in T9.4).

## Deliver
A workspace with no legacy surfaces and no dead endpoints.

## Non-goals
- **`publish-article.ts`, `admin-workflow-lock.ts`, `save-json-blob.ts` + its MCP surface are NOT deleted** (off-limits; agent-facing until Wolf retires them separately). No deletion of the `workflows` blob store data.

## Acceptance criteria
- Per group: zero importers remain (grep evidence in the task log); tests + check + full build green after each commit.
- Off-limits files verified byte-untouched (`git diff --stat` shows no changes to them).
- Manual: every deleted route 404s or redirects sensibly; the new maintenance page works Owner-only.
- Committed to the working branch (one commit per group). No PR unless asked.
