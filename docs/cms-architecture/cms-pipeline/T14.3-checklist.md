# T14.3 — the human-gate checklist, instantiated for `platform`

Everything an agent can do without account authority is **done and committed**
(T14.2 + the T14.3 prep commit). This file is the other half: the exact actions
that need Wolf's Netlify token and GitHub admin rights, in order, with nothing
else to read.

Two sittings' worth of work, assessed ~1 hour total (ruling R5).

---

## Before the sitting — one agent task that is NOT account authority

**`netlify/functions/mcp.ts` is not a shim.** It is a 4,533-line implementation
living in `netlify/functions/`, bound to Dr-Lurie, and it has no core factory.
The same is true of `save-json-blob.ts` (2,349 lines) and
`verify-article-images.ts` (476 lines). The other 32 functions are factories in
`packages/core/server/functions/` and `create-site` now generates a per-site
shim for each.

**Consequence:** the platform site will deploy, serve `/admin`, and serve every
governed verb — but **`/mcp` will 404 on it** until `mcp.ts` is extracted into
core behind a `createHandler(siteBinding)` factory like its 32 siblings.

That is ordinary agent work, not a gate. It should land *before* the sitting so
step 5 below can actually pass. `publish-article.ts` and `admin-workflow-lock.ts`
stay off-limits and stay Dr-Lurie-bound — nothing in W14 needs them on platform.

---

## Step 1 — create the Netlify project (Wolf, token)

Either run this yourself with a token that can create sites:

```
node packages/core/cli/create-site.mjs --name platform --netlify-token $NETLIFY_API_TOKEN
```

…or hand the agent the token and it runs it. The command is idempotent about
the directory (`sites/platform/` already exists and is left untouched); the
token half creates the Netlify site, probes-provisions this site's own blob
stores, and pushes the auto-mintable secrets straight to the site's env store.
Generated secret VALUES are never printed or written to disk.

Project name is your pick — `platform`, or a prefixed variant if the bare name
is taken.

### Then, in the new project's settings — three things, once

1. **Base directory → `sites/platform`.** This is what makes Netlify read
   `sites/platform/netlify.toml` instead of the repo-root one, so it builds the
   platform entry into the platform `dist` with the platform function shims.
   Without it the project silently builds Dr-Lurie.
2. **Build command and publish directory** come from that file — confirm they
   read `npx astro build --config sites/platform/astro.config.ts` and
   `sites/platform/dist`.
3. **Enable Identity** (Netlify Identity → Enable). Then set `IDENTITY_URL` to
   the new site's `/.netlify/identity` and add yourself to `ADMIN_EMAILS`.

### By-hand env values the token step cannot mint

The CLI prints the full table with a ✓/☐ per row. These are the ☐ ones:

| Variable | Value |
| --- | --- |
| `NETLIFY_BUILD_HOOK_URL` | create a build hook on the new project, paste its URL |
| `GITHUB_REPOSITORY` | the repo `owner/name` — **after** the rename in step 4 |
| `GITHUB_BRANCH` | `main` |
| `GITHUB_CONTENT_TOKEN` | write token for the repo (the fleet machine account is fine) |
| `GITHUB_COMMIT_AUTHOR_NAME` / `_EMAIL` | committer identity |
| `IDENTITY_URL` | the new site's `/.netlify/identity` |
| `ADMIN_EMAILS`, `ROLE_EMAILS_*` | your address |
| `MCP_HTTP_AUTH_TOKEN` | a fresh per-site key |
| `ARTIFACT_URL_INGEST_ALLOWED_HOSTS` | policy choice; empty is fine to start |

Reuse the FLEET-SHARED values verbatim — do not mint new ones:
`PDF_TOOL_STORAGE_SITE_ID`, `PDF_TOOL_STORAGE_TOKEN`, and the AI provider keys.

Also add `SECRETS_SCAN_OMIT_KEYS = "GITHUB_REPOSITORY"` — the same reason it is
on the Dr-Lurie project: the value is a plain typeable string and the scanner
fails the whole build on any appearance of it.

---

## Step 2 — seed drive through the front door (agent, once the site is up)

The five committed exports under `sites/platform/data/site/` are **bootstrap
stubs**, not objects: nothing in the store backs them. The agent creates the
real records through MCP, drills each permitted action, publishes, releases —
and the publish overwrites every stub with a genuine derived export. Needs the
site's URL and its `MCP_HTTP_AUTH_TOKEN`.

## Step 3 — rename the repo (Wolf, one click)

GitHub → Settings → rename to `platform`. GitHub redirects web, git, and API
traffic, and Netlify follows the repo by ID, so nothing breaks on the click.

Then the agent: updates `GITHUB_REPOSITORY` on **both** Netlify projects,
triggers one deploy each to verify the linkage, sweeps docs/scripts for
stragglers, and records the refs.

**The tree is already clean of the literal repo string** — one occurrence, in
`CLAUDE.md`, inside the warning that explains the rule. Nothing else to sweep
before the click.

## Step 4 — verify (agent)

- Platform renders its starter pages at its own URL.
- `/admin` loads and you can sign in (T14.0's fix is live).
- `/mcp` answers `ping` and `tools/list` — **conditional on the mcp.ts
  extraction above**.
- One deploy on each project after the rename, both green.

---

## What is explicitly NOT in this sitting

No DNS, no custom domain. **`drluriescience` is not renamed** (R4): it is
Dr-Lurie's project, correctly named, and renaming it would break every MCP
connector and env URL for zero benefit.
