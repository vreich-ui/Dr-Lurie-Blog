# T9.17 — CMS Agents hub

**Phase 9 · depends_on: T9.14 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** —.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §2 (route map row) + §4, the T9.13 chat verbs (`create_chat`/`list_chats`), the T9.14 chat components.

## Context
Cross-object conversations — "create a landing page for the retinol line," "make a new section template from the About hero" — the surface for work that isn't scoped to one existing object. Requirement 5's home: create reader-facing pages, section templates, page templates through agents.

## Scope — files to create/modify
- `src/pages/admin/agents.astro` (NEW, on AdminLayout) — session list (human titles, outcome chips: created X / published Y / no changes), resume, new-chat starters: **New article**, **New page from template** (agent walks REUSE-FIRST: inventory recipes → dry-run instantiate card → confirm), **New section template**, **Retheme** (Owner — dry-run `apply_theme` diff card).
- Chat-title generation (first-message summarization or explicit naming) so the list never shows raw chat ids.
- Creation flows route the finished object to its `/admin/content/<id>` workspace.

## Deliver
A hub where the §4 creation tools are exercised conversationally, dry-run-first, with results landing as normal governed objects.

## Non-goals
- No studio gallery (T9.18). No new tools beyond the T9.13 registry.

## Acceptance criteria
- Manual: "new page from template" starter walks inventory → dry-run card → confirm → object exists, opens in its workspace.
- Session list shows human titles + outcomes; no raw ids.
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
