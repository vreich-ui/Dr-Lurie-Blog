# T9.5 — Admin administration page + invite flow

**Phase 9 · depends_on: T9.4 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T9.7.

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §2 + §8, `netlify/functions/admin-users.ts` (T9.4), `netlify/lib/admin-auth.ts` (how `context.clientContext` arrives), GoTrue admin API invite semantics (`/.netlify/identity/admin/users` with the short-lived admin token at `context.clientContext.identity`).

## Context
The "assign admin rights to others" requirement: an Owner-only members page with a standard invite → accept → active lifecycle. No new secrets — the invite uses the Netlify-injected identity admin token.

## Scope — files to create/modify
- `netlify/functions/admin-users.ts` — add `invite` verb (Owner): create store record (`status: 'invited'`, `invited_by`) + GoTrue admin invite; idempotent on re-invite; audit entry.
- First-login activation: on `me`, flip `invited` → `active`, stamp `user_id` + `last_seen_at`.
- `src/pages/admin/settings/admins.astro` (NEW, on AdminLayout) + `src/components/admin-ui/` additions if needed — members DataTable (avatar, name, role badge, status pill, last seen), invite Dialog (email + role), role-change / disable with ConfirmDialog, per-member audit Drawer.
- Tests: mocked GoTrue admin API (invite success/failure, idempotency), activation flip, Owner-only enforcement (admin gets 403).

## Deliver
A working Owner-facing admin-administration page; Admin-tier users see it hidden AND get server 403s.

## Non-goals
- No profile page (T9.6). No email templating beyond Netlify Identity's stock invite.

## Acceptance criteria
- Tests above green; `npm run check` + `npm test` green; public build-diff EMPTY.
- Manual: invite dialog → store record `invited` + GoTrue call fired (mock/dev); role change + disable append audit entries and render in the drawer.
- Committed to the working branch. No PR unless asked.
