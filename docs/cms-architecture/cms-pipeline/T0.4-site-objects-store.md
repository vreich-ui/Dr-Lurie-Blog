# T0.4 — site-objects Store + Key Scheme

**Phase 0 · depends_on: T0.1 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T0.5, T0.8.

## Read first
`docs/cms-architecture/01-audit.md` §1.1 (existing blob-store accessor pattern) and `02-architecture-and-schema.md` §1 (the derived-export path layout the key scheme mirrors).

## Context
CMS objects live in Netlify Blobs (the source of truth). This task adds the storage accessor and the key-naming scheme, following the exact pattern the existing blob accessors use — including strong consistency.

## Scope
- `netlify/lib/blob-store.ts` — **modify**: add `getSiteObjectsBlobStore`, with strong consistency, matching the existing accessors (A§1.1). Don't change existing accessors.
- `netlify/lib/object-store-keys.ts` — **create**: record key `objects/{type}/by-id/{id}.json`; marker-blob status indexes mirroring the existing `workflows/index/...` convention (D§1).

## Deliver
The store accessor and a pure key-construction module. Extend `getCoreBlobStoreSourceDiagnostics` so the diagnostics endpoint lists the new store.

## Non-goals
- No verb endpoints yet (T0.8) — this is storage plumbing only.
- Do not alter existing blob accessors' behavior.

## Acceptance criteria
- Unit tests over key construction (every type produces the expected path; status-index markers correct).
- The diagnostics endpoint lists the new `site-objects` store.
- Committed to the working branch. No PR unless asked.
