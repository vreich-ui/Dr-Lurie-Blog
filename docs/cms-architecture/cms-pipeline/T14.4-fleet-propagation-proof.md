# T14.4 — Fleet propagation proof (subsumes old T11.11's substance)

**Phase 14 · depends_on: T14.3 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T14.5 (soft), T14.9.

## Read first

Ruling doc (supersessions), `T11.11-second-site-acceptance.md` (the four
original proofs), `scripts/ci/discover-fleet-matrix.mjs`.

## Scope

1. Agent MCP round-trip on the PLATFORM site: one page created via MCP →
   patched → published → released; its `object_inventory` returns it;
   Dr-Lurie's store shows zero change.
2. One trivial core commit + one real small core fix → fleet CI builds
   BOTH sites (discover-fleet must now report two sites — first live
   two-site matrix run; watch it, this is the previously-unverified
   Actions path).
3. Dr-Lurie: no *unintended* change (informational build-diff reviewed;
   byte-identity not demanded, R3).
4. Record all four proofs with commit/deploy refs in state-of-play.

## Acceptance criteria

- Two isolated tenants on one canonical core, propagation demonstrated
  and recorded. Committed `T14.4: …`; no PR.
