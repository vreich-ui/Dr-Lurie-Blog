# T9.10 — Live preview pane

**Phase 9 · depends_on: T9.9 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** T9.14 (layout completeness).

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §4 (preview strategies), `src/lib/article-object/render-nodes.ts`, `src/lib/richtext/` renderers, `src/lib/edit-mode/preview.ts` (the canvas field-swap approach), the theme/site body schemas.

## Context
The workspace's right-hand pane: see what you're editing, per type, re-rendered on every accepted patch.

## Scope — files to create/modify
- `src/components/admin-ui/ObjectPreview.tsx` (+ per-type strategy modules):
  - page/section: iframe of the rendered route with a draft-overlay query param resolving the working copy (reuse the edit-mode draft resolution approach; fall back to published render + "unpublished changes" notice if a draft overlay is out of scope for v1 — note the choice in the task log).
  - content_item: client render of **public copy only** via `render-nodes` — the leak rule applies to the workspace too.
  - theme: palette swatch board + typography specimen from `tokens`.
  - product: price card + commerce-mode/gate summary.
  - navigation/taxonomy/site: structured, human-readable tree views.

## Deliver
Preview renders for all ten types and refreshes after each `EditSession.patch` accept.

## Non-goals
- No pixel-true draft SSR (out of scope repo-wide; the canvas has the same limitation). No editing from the preview.

## Acceptance criteria
- **Leak-rule test:** a content_item fixture with `private.strategy`/agentNotes renders zero `private.*` strings into the preview DOM.
- Manual: patch a field → preview updates without full page reload.
- `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
