# T9.26 — Agent roster & assignment UI + canvas Ask-AI re-point

**Phase 9 · depends_on: T9.14 · mode: auto · model: Opus 4.8 · effort: medium**
**Blocks:** — (W9.e).

## Read first
`docs/cms-architecture/10-admin-workspace-plan.md` §4a (dedicated agents — Wolf's 2026-07-16 amendment) + §6 (agent-profiles row), `netlify/lib/agent/profiles.ts` (T9.13 — store, schema, resolution chain, seeded defaults), `netlify/lib/ask-ai-object.ts` + `netlify/functions/admin-ask-ai-object.ts` (the canvas suggestion path to re-point).

## Context
Wolf's ruling: each object may have its own dedicated agent, and an admin changing an object must ALWAYS be connected to it. T9.13 built the runtime half (profiles store + resolution + both provider adapters). This task builds the management surface and closes the loop on the canvas, so every change surface talks to the object's agent.

## Scope — files to create/modify
- `/admin/agents` roster section (extend `src/pages/admin/agents.astro`): profile cards (name, avatar, provider + model badge, status); create/edit dialog (name, avatar via artifact upload, **provider: anthropic | openai**, model, system prompt, tool-autonomy overrides); Owner-only manage, Admin read.
- Assignment surfaces (Owner): per-object "Dedicated agent" selector in the object workspace header/Details, per-type defaults + site default in the roster; writes to the T9.13 assignments doc.
- Chat identity: object workspace + hub threads render the resolved AgentChip (T9.14 component) from the run record.
- `netlify/lib/ask-ai-object.ts` / `admin-ask-ai-object.ts` — resolve the object's profile and use its provider/model for the single-shot suggestion (copy-only guard and schema behavior unchanged); remove the hardcoded provider/model constants.
- Tests: assignment CRUD Owner-only (Admin 403); resolution reflected in a new chat's run record; ask-ai uses the assigned profile (mock both providers).

## Deliver
Owners create and assign dedicated agents; the workspace chat AND canvas Ask-AI for an object both speak through its assigned agent, visibly identified.

## Non-goals
- No new tools or autonomy semantics (T9.13/T9.15 own those). No per-agent credentials (OQ-3). No agent-to-agent flows.

## Acceptance criteria
- Manual: assign agent A (anthropic) to one object and agent B (openai) to its type → object chat resolves A, a sibling object resolves B, unassigning falls back correctly; canvas Ask-AI on each object hits the matching provider (verified via mocks/logs).
- All tests green; `npm run check` + `npm test` green; public build-diff EMPTY.
- Committed to the working branch. No PR unless asked.
