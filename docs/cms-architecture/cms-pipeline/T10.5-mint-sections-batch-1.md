# T10.5 — Mint section types, batch 1 (first half of the ratified list)

**Phase 10 · depends_on: T10.4 · mode: auto · model: Opus 4.8 · effort: high**
**Blocks:** T10.6, T10.8.

## Read first
`docs/cms-architecture/design-vocabulary-gaps.md` (the RATIFIED section — the list is Wolf's, not this brief's), `docs/cms-architecture/11-platformization-plan.md` §1.2, `src/schema/bodies/section-v1.ts`, `src/lib/registry/components/` (a registry module + component per type — copy the `testimonial`/`content_split` pattern), `src/lib/registry/components/registered-types.ts`, `docs/cms-architecture/block-tree.md` (container bounds, if a minted type holds cells).

## Context
Each mint is the proven one-module recipe: one union member + one registry module (schema binding, editor hints, `useWhen`, `defaultData`) + one component, surfaced automatically in `object_contract.section_types` / `registry_get`. Candidate shapes (final list per T10.4): `media`, `brand_row`, `stats`.

## Scope — files to create/modify
- For each ratified type in batch 1 (first ~half of the list, ratified order): `src/schema/bodies/section-v1.ts` union member (`.strict()`, bounded fields only — enums/limits per rule 6); ✚ `src/lib/registry/components/<type>.ts`; ✚ `src/components/sections/<Type>.astro`; registration in `registered-types.ts` + the component registry (the total `Record` forces lockstep — a miss is a compile error).
- Container-bearing types declare `allowedChildren`/`childCount` bounds; image-bearing fields follow the artifact-trust/sanctioned-host rules (see `bio.ts` portrait precedent).
- Tests per type: schema accept/reject, renderer smoke (the showcase-page pattern), placeability (`isStandalonePlaceableSectionType` true), editor hints present.

## Deliver
Batch-1 types fully registered, contract-visible, render-proven — usable on any page by any agent, used by no page yet.

## Non-goals
- No page/seed edits. No composite/nested arrangement. Nothing not on the ratified list.

## Acceptance criteria
- `npm run check` + `npm test` green; `astro check` 0 errors; build-diff EMPTY (nothing renders them yet); the object-showcase fixture updated to pin the grown union.
- Committed `T10.5: …`; no PR.
