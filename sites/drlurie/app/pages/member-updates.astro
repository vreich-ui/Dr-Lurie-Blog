---
/**
 * Member Updates (T4.3 cutover) — delegates to the shared PageObjectRenderer,
 * which loads the published page_member_updates object and renders its `lede`
 * section. Copy lives in the CMS object; the markup is unchanged.
 */
import PageObjectRenderer from '~/components/cms/PageObjectRenderer.astro';
---

<PageObjectRenderer objectId="page_member_updates" />
