---
/**
 * Newsletter (T4.3 cutover) — delegates to the shared PageObjectRenderer, which
 * loads the published page_newsletter object and renders its `lede` section.
 * Copy lives in the CMS object; the markup is unchanged. (The signup form is a
 * later addition, S-3 — this page is a lede today, exactly as audited.)
 */
import PageObjectRenderer from '~/components/cms/PageObjectRenderer.astro';
---

<PageObjectRenderer objectId="page_newsletter" />
