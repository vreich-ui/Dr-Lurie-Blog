# T0.2 — Per-Type Body Schemas

**Phase 0 · depends_on: T0.1 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T0.3, T0.6, T0.7, and all materializers.

## Read first
`docs/cms-architecture/02-architecture-and-schema.md` §3.2–3.8 (the per-type schemas), plus `cms-architecture-consolidated.md` §4 (the amendment log — every amendment below must be present) and §2 (the shared_ref vs independent-instance distinction).

## Context
The body is the type-specific payload inside the envelope from T0.1. This task defines one body schema per object type. Several amendments were discovered during later design sessions and must be baked in now — missing one means re-touching these files later.

## Scope — files to create
- `src/schema/bodies/navigation-v1.ts`
- `src/schema/bodies/page-v1.ts`
- `src/schema/bodies/section-v1.ts` (includes the shared-section wrapper + the `SectionInstance` union)
- `src/schema/bodies/template-v1.ts`
- `src/schema/bodies/site-v1.ts`
- `src/schema/bodies/taxonomy-v1.ts`

## Deliver
D§3.2–3.8, with **all committed amendments**:
- **M-1**: `NavItem.description`
- **M-2**: `groups[].slot`
- **M-5**: `groups[].target` — stored but NOT rendered as a link (C§1.2). Store it; don't wire it to anything.
- **M-7**: `NavItem.icon` and `NavItem.ariaLabel`
- The transitional `NavTarget {kind:'route', href}` union variant (this is deliberate per Gap Note 2's lifecycle — not a placeholder to "fix").
- The `shared_ref` union member in the section schema (D§3.5).
- The transitional `content_grid` `static` cards variant (needed for the P3 interim step).

**Deliberately NOT here: M-8** (the grid manual+fallback refinement). That is T3.3. Do not add it now even though it's a settled decision — it lands with the homepage work.

## Non-goals
- No ID validation logic (T0.3), no patch ops (T0.6), no validation pipeline (T0.7).
- Do not implement M-8.

## Acceptance criteria
- Unit tests per type; a fixture seed of each body parses cleanly.
- `route`-kind nav targets are accepted; unknown target kinds are rejected.
- Committed to the working branch. No PR unless asked.
