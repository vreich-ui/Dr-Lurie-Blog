# T0.9 — MCP Tool Exposure

**Phase 0 · depends_on: T0.8 · mode: auto · model: Sonnet 5 · effort: medium**
**Blocks:** T0.11, and all Phase 3+ agent-facing work.

## Read first
`docs/cms-architecture/01-audit.md` §1.8 (the existing MCP proxy pattern to mirror).

## Context
This exposes the object verbs as MCP tools so agents can actually call them. It reuses the existing proxy pattern and must not disturb the existing article MCP tools.

## Scope
- `netlify/functions/mcp.ts` — **modify**: add to `TOOL_DEFINITIONS` and the `callTool` dispatch **only**. No changes to existing article tools.

## Deliver
- Tools: `object_get / object_list / object_create / object_checkout / object_refresh_lock / object_checkin / object_patch / object_validate`, proxying to the same backing logic with the publish key injected (the existing proxy pattern, A§1.8).
- `registry_get` stub returning a static "not yet populated" response until P3 populates the component registry.

## Non-goals
- No publish/review tools (Phase 1).
- Do not modify existing article MCP tools in any way.

## Acceptance criteria
- MCP smoke tests mirroring the existing `tests/netlify/mcp-*.test.ts` patterns.
- `tools/list` includes the new tools.
- **The existing article tools' tests still pass, unmodified** — confirm explicitly.
- Committed to the working branch. No PR unless asked.
