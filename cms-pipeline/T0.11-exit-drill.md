# T0.11 — Phase 0 Exit Drill

**Phase 0 · depends_on: T0.8, T0.9 · mode: auto · model: Sonnet 5 · effort: high**
**This drill IS the phase's exit criterion — it's what proves Phase 0 is actually done.**

## Read first
`docs/cms-architecture/04-phased-plan.md` P0 exit criteria (round-trip drill, conflict-semantics parity, MCP reachability) — the drill must match these verbatim.

## Context
An end-to-end test proving the whole Phase 0 object store works as a system, not just as isolated units. If this passes, Phase 1 can start.

## Scope — files to create
- `tests/netlify/object-lifecycle.e2e.test.ts`

## Deliver
A scripted round-trip for **both** a `page` fixture and a `navigation` fixture:
`create → checkout → patch (then replay its inverse) → validate → get`.

Matching the P§ P0 exit criteria verbatim: the round-trip succeeds, conflict semantics (409/423) behave as specified when provoked, and the verbs are reachable via MCP.

## Non-goals
- Don't add new production functionality to make the drill pass — if something's missing, that's a signal an earlier task (T0.1–T0.9) was incomplete. Flag it; don't patch around it here.

## Acceptance criteria
- The drill passes end-to-end for both fixtures.
- The inverse-replay step confirms the object returns to its pre-patch state.
- Committed to the working branch. No PR unless asked.

## After this passes
Phase 0 is complete. Phase 1 (T1.1) can begin. If you're running the automation loop, this is a natural notify-Wolf checkpoint — a full phase boundary is a good place for a human to glance at what landed before the next phase starts.
